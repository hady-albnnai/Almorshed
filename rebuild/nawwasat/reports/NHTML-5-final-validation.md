# NHTML-5 — التحقّق النهائي (المطابقة الكاملة مع المصدر)

**المرحلة:** NHTML-5 (الأخيرة) · **التاريخ:** 2026-09-20 ·
**المصدر المجمّد:** `rebuild/nawwasat/source/original.docx` — sha256 `39dcae28ec74ad26520823b6057faeeefe9981ddd3c86a9f78244c27afd35d50` (5,193,418 بايت) ✅ لم يتغيّر (يُعاد التحقق آلياً في كل تشغيل).
**المُخرَج النهائي:** `rebuild/nawwasat/pdf/نوطة-النواسات.pdf` — 79 صفحة A4 (209.9×297.0 مم)، 4.81 م.ب، sha256 `ee3c9dd0…` · HTML: `html/index.html` (1,308,884 بايت، sha256 `ac892740…`).
**الأداة:** `tools/final_validation.py` (جديدة في هذه المرحلة) → `content/final-validation.json` + `reports/nhtml5-shots/`.

---

## 1. منهج التحقّق
لكل عنصر في المصدر مقابل ما ظهر فعلاً في PDF — لا بالعيّنة بل بالكامل:

| المحور | الطريقة |
|---|---|
| النصّ | تغطية حروف كل كتلة نصّية (591 كتلة) من `content/text.json` مقابل مُلتقَط نصّ PDF (تطبيع NFKC) |
| الصور | 78 ملف مصدر مقابل 75 صورة مُدرجة في PDF — بصمة متوسّطة 32×32 (ahash) لكل موضع |
| المعادلات والرموز | جدول مقابلة 22 رمزاً: نصّ HTML المعروض ↔ نصّ PDF + عدّ العناصر البنيوية (msqrt/mfrac/mover) |
| اللون | مقاطع النصّ الخضراء `#008000` (المحدّدات) + `#008000` داخل MathML |
| الترقيم | عدد الصفحات · وجود التذييل في كل صفحة · تسلسل الأرقام · مقاس الصفحة |
| بصرياً | تصيير كل الصفحات + 7 ألواح استعراض (12 صفحة/لوح) + لقطات تكبير موضعية |

## 2. النتائج — الحكم: **PASS (7/7)**

```json
{"text_complete": true, "images_present": true, "pages_clean": true,
 "footer_every_page": true, "source_untouched": true,
 "symbols_intact": true, "math_and_green_colors": true, "PASS": true}
```

**النصّ:** 591 كتلة مفحوصة · 0 كتلة ناقصة حرفاً · 0 حرف مفقود · 12,997 حرفاً عربياً في PDF.
(فروق `verify_html.py` على مستوى المحرف/الكلمة = تطبيع (تنوين/همزات) + عناصر تصميم مضافة، والمرجع الحاسم هو تغطية الكتل = 0 ناقص.)

**الصور:** 78 ملفاً = 75 مستعملاً + 2 نسخة `mc:Fallback` (image60/image590) + 1 يتيمة (image1.png, 15×15). المُدرج في PDF 75/75 على 48 صفحة، **75/75 مطابقة بصرية للمصدر (Hamming = 0)**، 75 ملفاً مميّزاً.

**المعادلات والرموز:** 1346 عنصر `math` · الجذور 142 `msqrt` · الكسور 717 `mfrac` · **الإشارات فوق الحروف `mover accent` = 55 = عدد `m:acc` في المصدر 55** · جدول الرموز 22/22 مطابق (لا فرق واحد):

| الرمز | HTML | PDF | | الرمز | HTML | PDF | | الرمز | HTML | PDF |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `±` | 2 | 2 | | `∑` | 9 | 9 | | `⟹` | 13 | 13 |
| `∓` | 15 | 15 | | `\|` (المحدّدات) | 10 | 10 | | `⇒` | 60 | 60 |
| `Δ` | 176 | 176 | | `⟸` | 11 | 11 | | `→` | 50 | 50 |
| `α` | 24 | 24 | | `⇐` | 49 | 49 | | `≤` / `≠` | 4 / 2 | 4 / 2 |
| `ω` `π` `θ` `φ` `Γ` | 260 / 298 / 251 / 106 / 18 | نفسه | | `√` | 0 | **142** | | `∝` `∆` `ℰ` | 0 | 0 ✅ (R3/R4 مُطبَّقتان) |

**اللون:** `#008000` داخل المعادلات = 12 (المحدّدات الخضراء) · مقاطع نصّ خضراء فعلية في PDF = 21 على الصفحات {3, 7, 9, 11, 26, 50} — طُوبِقت مع `x = | ± X_max |` و`F_max = | ∓k X_max |` … (لقطات `green-bars-page-00{3,7,9}.png`).

**الترقيم:** 79/79 صفحة عليها التذييل (سطر الأستاذ + رقم الصفحة) · الأرقام 1→79 متسلسلة · 0 صفحة فارغة · 0 صفحة داكنة · مقاس موحّد A4.

## 3. عيوب اكتُشفت في هذه المرحلة وأُصلحت (شكل فقط — لا محتوى)

1. **رؤوس أسهم مضخّمة:** مؤشّر السهم SGV كان `markerWidth=6` (٦× سماكة الخط) فظهرت رؤوس بعض الوصلات القصيرة ثنائية الاتجاه **مثلّثات سوداء كبيرة تُقرأ كنجوم (19×19 نقطة)** في الصفحات 3، 4، 23… — أُصلح بقراءة حجم الرأس من المصدر (`headEnd/tailEnd` → sm/med/lg = 2.2/3/5× سماكة الخط؛ الافتراضي med). النتيجة: رؤوس بقياسها الوظيفي (9.5 نقطة) على الصفحة 3، وحركة الفأرة A/B في اللقطة `/tmp/star3-after.png`.
2. **نجوم خمسية غير مرسومة:** أشكال `star5` الأربعة في المصدر (لها `fillRef=accent1` و`lnRef=accent1 shade 15%` بلا تعبئة صريحة) كانت تُرسَم كخط باهت. أصبحت تُرسَم نجمة خمسية بلون السمة `#156082` وحدّ `#04141C` — أربع نجوم في الصفحات 2 و4 (لقطة `/tmp/star-accent-2-396.png`).
3. **تصحيح قياسين زائفين في الفحص:** (أ) عدّ الرموز كان يتضخّم بوجود `alttext` على عناصر `math` (٤ و30 أصبحت ٢ و15 عند العدّ المعروض) — يُجرَّد `alttext` الآن قبل العدّ؛ (ب) مقياس `overWide` كان يقارن بحاوية `inline` عرضها صفر → 0 بعد التصحيح.

## 4. مسح الحبر والهوامش (توضيح)
فحص الحبر (72dpi) يجد حبراً حتى y≈290.3 مم في كل صفحة لأنّ **التذييل يُرسم عمداً داخل الهامش السفلي** عبر قالب تذييل Chromium (لا يُقصّ محتوى)، وخطّ إطار الصفحة عند x≈14.7 مم. الفحص البُنيوي الحاسم (check_print + pdf-check) يعطي: **0 قصّ · 0 تجاوز أفقي · 0 جدول عريض · 0 صفحة فارغة · 0 تعبئة داكنة كبيرة**.

## 5. المطابقة مع المصدر — الأعداد الختامية
- الفقرات 1910/1910 · المعادلات 1346/1346 (MathML أصلي) · الأشكال 399/399 · الجداول 6/6.
- الرسم: 96 شكلاً متصلاً (SVG) + 162 تسمية نصّية + 34 قوساً + 24 إطاراً/صندوقاً + 4 نجوم + 75 صورة + 7 مجموعات.
- تصحيح R1–R14: `max→maX` استُكمل في هذه المرحلة (6 مواضع مقسّمة على مقطعين كانت تفوت القاعدة) ⇒ **`max` الصغيرة = 0 و`maX` = 296** في PDF.
- التزام: **لا تغيير في كلمة/معادلة/رسم/لون** — كل ما سبق تعديلات شكل (تخطيط/ترقيم/رسم رؤوس ونجوم كانت ناقصة أو مضخّمة).

## 6. القيود المعلومة (شفافية)
- 4 أشكال `star5` كانت بلا شكل سابقاً — الآن مرسومة بلون السمة (accent1) لا بلون مخصّص من الأستاذ (المصدر لا يحمل تعبئة صريحة).
- ألوان بعض الأشكال المعتمدة على `wps:style` (242 شكلاً) لا تُترجم إلى لون السمة حرفياً في التصميم الجديد: التسميات تُعرض كصناديق فاتحة موحّدة (قرار تصميمي مثبَّت في NHTML-2 ومعتمد من المالك).
- التذييل يحمل نصّ الأستاذ مع رقم الصفحة المُولَّد ديناميكياً بدل حقل PAGE في Word (وثيقة `NHTML-3/4`).

## 7. المخرجات
- `content/final-validation.json` — كل القياسات أعلاه بصيغة آليّة (verdict 7/7).
- `reports/nhtml5-shots/` — `sheet-01…07.png` (كل الصفحات 79)، `page-001/008/030/060/079.png` (100dpi)، `green-bars-page-003/007/009.png` (130dpi).
- إعادة الإنتاج: `python3 rebuild/nawwasat/tools/final_validation.py`.
