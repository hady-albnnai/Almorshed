# نموذج الفروع وحالة العمل — Almorshed / المرشد في الفيزياء

> آخر تحديث: 2026-09-23

## ⛔ `main` — مُجمّد (FROZEN)

**لا تعمل على `main`.** فرع `main` مُجمّد رسمياً بانتظار عودة الإشراف العلمي
(الأستاذ فداء مأمون البني). لا تُدمج فيه أي أعمال تطويرية جديدة، ولا تُدفع إليه
كومِتات، حتى يعود الإشراف العلمي ويُراجع المادة.

آخر كومِت مستقر على `main`: تمّ تجميده عند هذه النقطة.

## ✅ `dev/self-content` — فرع التطوير النشط

**كل العمل الجديد يجري هنا.** أُنشئ هذا الفرع لإكمال تطوير التطبيق **دون إشراف
علمي** (بقرار المالك، 2026-09-23)، اعتماداً على:

- **المنهاج = الكتاب الوزاري بالكامل** (منهاج دمشق للبكالوريا العلمية، الفيزياء،
  2026-2027).
- **المادة العلمية (الأسئلة والمسائل)** تُبنى من **أسئلة الدورات الوزارية السابقة
  + سلالم التصحيح الوزارية**.

### قرارات هذا الفرع (تختلف عن `main`)

1. **إزالة الإسناد للإشراف العلمي:** يُحذف ذكر «الأستاذ فداء مأمون البني» من
   واجهات التطبيق (شاشة التفعيل، صفحة «عن التطبيق») والاختبارات المرتبطة به.
2. **بوابة اعتماد جديدة «مصدر وزاري»:** يُعتمد المحتوى المبنيّ من الدورات وسلالم
   التصحيح عبر علم اعتماد جديد بدل انتظار توقيع الأستاذ.
3. **إدراج نصّ الكتاب الوزاري كاملاً** داخل التطبيق (تجاوز قرار «الإحالة للصفحة
   فقط» السابق).

### عند عودة الإشراف العلمي

يُعاد تقييم دمج `dev/self-content` في `main`، ويُراجَع المحتوى المبنيّ ذاتياً،
وتُعاد النظر في قرارات الإسناد والاعتماد أعلاه.

---

*هذا الملف موجود على كلا الفرعين ليكون واضحاً أن `main` مُجمّد وأن التطوير يجري
على `dev/self-content`.*
