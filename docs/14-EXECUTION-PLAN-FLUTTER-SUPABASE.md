# ١٤ — خطة التنفيذ الكاملة — Flutter + Supabase — فيزيا كلاش (Clash of Physics)

> **الحالة:** معتمدة كدليل تنفيذ إلزامي · **التاريخ:** 2026-09-11 · **القرار الارتباطي:** ٤٨ في `01-PLAN-COMPLETE.md`
> **الغاية:** خطة من الألف إلى الياء مصمَّمة لتقيد **أي محادثة مستقبلية** — لا تخطٍّ ممكن لأي نقطة من قراراتنا ١–٤٧.
> **القاعدة الأم (§٤.١ من الخطة):** قبل كل مهمة جديدة بحث معمّق أولاً. هذه الخطة نفسها بُنيت على ٣ جولات بحث 2026-09-11.

---

## ٠. دستور الجلسة — كيف تعمل أي محادثة دون تخطي شيء ⚖️

> **النسخة القانونية الأم:** `00-CONSTITUTION.md` بجذر المستودع — تُقرأ أولاً في كل محادثة. هذا §٠ ملخصها التشغيلي داخل الخطة، وعند أي تعارض تُقدَّم النسخة الأم.

### ٠.١ بروتوكول بداية كل محادثة (إلزامي، بلا استثناء)
1. اقرأ **§١٢ خريطة التقدم** أدناه ← حدّد **أول مهمة غير مشطوبة `[ ]`** بالترتيب.
2. اقرأ في `01-PLAN-COMPLETE.md` §القرارات أي رقم قرار ذكرته المهمة.
3. اعمل على **هذه المهمة فقط** — لا تنقل ولا تعيد ترتيباً ولا «تحسّن» شيئاً خارجها.
4. إن طلب صاحب المشروع شيئاً خارج المهمة ⇒ نُفَّذ، ثم يُسجَّل **كقرار جديد برقم** في الخطة، ثم نعود للمهمة.

### ٠.٢ بروتوكول نهاية كل محادثة (إلزامي)
5. اشطب المهمة: `[x]` + `(كومِت <hash> — <تاريخ>)`.
6. حدّث **§١٣ الحالة الحالية** (٣ أسطر: أُنجز / التالي / معوّقات).
7. كومِت + دفع برسالة تبدأ بمعرف المهمة: `F3.4: ...`.
8. **تحقق grep من القرص** بعد كل كتابة (لا ثقة برسالة نجاح الأداة) — وقراءة `git ls-remote` بعد الدفع.

### ٠.٣ قواعد منع التخطي الصارمة
- المهام **تسلسلية** داخل مرحلة، والمراحل شبه تسلسلية (يُسمح بتوازي واضح فقط إذا وثّقه صاحب المشروع).
- أي عائق ⇒ يُسجَّل في **§١٤ المعوّقات** ويُسأل صاحب المشروع — لا حلول تخمينية (قاعدة المشروع).
- أي شيفرات تُكتب يجب أن تحقق «تعريف الإنجاز» الخاص بمهمتها — لا إنجاز جزئي بلا تسجيله.
- النموذج الحي `ui-mockup/index.html` هو **المواصفة البصرية المرجعية** — أي شاشة تُبنى تُقارن به.
- **مصفوفة التتبع** (§١١) تربط كل قرار بمهمته — لا يُعلَن اكتمال مرحلة قبل أن تكتمل مهام قراراتها كلها.

---

## ١. القرار ٤٨ — الرزمة التقنية (مبني على بحث 2026-09-11)

| الاختيار | البرهان البحثي |
|---|---|
| **Flutter** (أحدث مستقر — يُثبَّت في pubspec عند F0.2) | هدف أندرويد أولاً + RTL أصلي + خطوط مضمّنة |
| **Drift (SQLite)** قاعدة الحقيقة المحلية | دليل 2026 «Maintenance-First»: «ابدأ بـ Drift»؛ Isar حمرُ صيانة، Hive للإعدادات فقط [1](https://luci-studio.com/blog/the-flutter-local-database-landscape-in-2026-a-maintenance-first-guide-fe6d267c/) [2](https://www.fluttersolution.com/2026/08/flutter-interview-prep-22-offline-first.html) |
| نمط **اكتب محلياً ← زامن لاحقاً** + طابور عمليات + WorkManager | دليل Offline-First الشامل 2026 [3](https://flutterstudio.dev/blog/offline-first-flutter-drift.html) |
| **Supabase**: Postgres + Auth + **Realtime Broadcast/Presence** + Edge Functions | Broadcast ‏<50ms للـgame state حصراً؛ Presence للتتبع؛ قنوات خاصة إلزامية بإذن RLS على `realtime.messages` [4](https://supabase.com/docs/guides/realtime/authorization) [5](https://www.agilesoftlabs.com/blog/2026/05/supabase-realtime-in-production-what) |
| **cryptography** (Dart — Ed25519) لتوقيع الجهاز والفحص المحلي؛ ed25519_edwards احتياط (متجمّدة 0.3.1 — 2021) ومتجهات F1.3 الذهبية تضمن تكافؤ التوقيعات [6](https://fluttergems.dev/packages/ed25519_edwards/) |
| Dart int الأصلي = 64 بت على الجوال (بتّي مطابق لمرجع BigInt) | يخدم محرك docs/12 الحتمي — تؤكده متجهات golden في F1.3 |

**ممنوعات معمارية:** قراءة UI من الشبكة مباشرة (القراءة من Drift دائماً) · `Math.random`/`Random()` في أي مسار لعبة · مفاتيح service_role في التطبيق · قنوات Realtime عامة في الإنتاج.

## ٢. الحزم المعتمدة (pubspec عند F0.2 — تُثبَّت أحدث نسخة متوافقة)

`supabase_flutter` · `flutter_riverpod` · `drift` + `drift_dev` + `build_runner` + `sqlcipher_flutter_libs` (تشفير القاعدة) · `cryptography` (Ed25519 — صيانة نشطة) · `crypto` (SHA-256) · `workmanager` (مزامنة خلفية) · `connectivity_plus` · `flutter_secure_storage` (مفاتيح الجهاز) · `mobile_scanner` (QR) · `nearby_connections` (يُتحقق من صحته ومستوى صيانته عند F5.4 — خطة بديلة: Hotspot يدوي قرار ٢٥) · `flutter_math_fork` (معادلات — **بوتقة اختبار S1–S4** قبل الاعتماد) · `shared_preferences` · `http`.

## ٣. هيكل المستودع المستهدف (يُبنى تدريجياً في نفس repo «Almorshed»)

```
Almorshed/
├─ app/                    # مشروع Flutter (F0.2)
│  ├─ lib/
│  │  ├─ core/            # rng/ الحتمي · fsrs/ · crypto/ · db/ (Drift) · theme/ (رموز docs/13)
│  │  ├─ features/        # curriculum/ training/ cards/ lab/ duels/ league/ account/ activation/
│  │  ├─ sync/            # طابور العمليات + محرك المزامنة
│  │  └─ main.dart
│  ├─ assets/fonts/       # Cairo + Tajawal (OFL — docs/13)
│  ├─ assets/content/     # محتوى مولّد ومعتمد (JSON)
│  └─ test/               # golden vectors + FSRS + دفتر XP + widget tests
├─ supabase/
│  ├─ migrations/         # SQL ترحيلات مرقمة
│  ├─ functions/          # Edge Functions (Deno/TS)
│  └─ seed/
├─ tools/                 # أدوات قائمة (توليد/مراجعة) + أداة المكتب
├─ ui-mockup/             # النموذج المرجعي (يبقى)
├─ docs/                  # 12-ALGORITHM · 13-DESIGN-IDENTITY · هذه الخطة
└─ 01-PLAN-COMPLETE.md    # القرارات ١–٥٠
```

## ٤. قواعد البيانات أوف لاين أولاً (Drift) — من دليل 2026 [3]

- UUID مفاتيح أساسية (لا auto-increment) · كل جدول `created_at/updated_at` · حذف ناعم `is_deleted`.
- **كل كتابة = معاملة واحدة**: كتابة Drift + إدخال بطابور العمليات معاً.
- `NativeDatabase.createInBackground` (عزل خلفي — لا jank).
- حل التعارض: LWW عبر `updated_at` (مقبول: بيانات مستخدم واحد غالباً).
- مؤشر حالة المزامنة ظاهر للمستخدم (شريط «معلّقات: ٣ — ستُرفع عند الاتصال»).
- سقف إعادة محاولة 5 ثم تسجيل دائم — لا محاولات أبدية.

## ٥. مخطط Supabase (يُنفَّذ كترحيلات F4.2–F4.4)

| الجدول | الغرض | RLS جوهرية |
|---|---|---|
| `profiles` | هوية الطالب، displayName، فصيلته | يقرأ/يعدّل نفسه فقط |
| `devices` | جهاز لكل صف: pubkey Ed25519، بصمة، حالة | مالك الجهاز فقط |
| `licenses` + `activations` | كود (hash)، صلاحية، سقف جهازين، روابط الأجهزة | قراءة مالك فقط؛ الإصدار service-role |
| `server_questions` | مرآة البنك المعتمد + الإجابات (سرية) | **بلا وصول مباشر للعميل** — عبر Edge Functions حصراً |
| `xp_events` | الدفتر الموقَّع (seq, hash, sig, payload) | إدراج مالك؛ لا تعديل؛ التحقق Edge Function |
| `duels` + `duel_players` | بذرة، نقلية، حالة، نتائج موقعة | المشاركان فقط |
| `duel_access` | بوابة قنوات Realtime الخاصة (user↔duel) | تُغذي RLS على `realtime.messages` [4] |
| `league_weeks` + `league_members` | نوافذ الاثنين→الأحد، مجموعات ~٣٠ بمستوى متقارب | قراءة أعضاء دوريك فقط |
| `seasons` + `certificates` | موسم ينتهي قبل الامتحان بشهر + شهادات موقعة | قراءة عامة للشهادة عبر معرفها |

**Edge Functions:** `license_activate` (سقف الجهازين) · `verify_xp_events` (سلسلة+توقيع ثم إيداع بالدوري — رفض أي شيء آخر) · `duel_finish` (إعادة تشغيل المحرك حتمياً في السيرفر ومطابقة — السلطة العليا ضد الغش) · `league_rollup` (أسبوعي مجدول) · `cert_issue` (نهاية موسم).
**Realtime:** قناة خاصة لكل مبارزة `duel:{id}` — Presence (حضور/جاهزية) + Broadcast (حركة الإجابات <50ms) — limits الحرة 200 متزامن (كافية للانطلاق؛ Pro عند النمو) [5].

## ٦. المراحل والمهام (ID ثابتة — تُشطب هنا فقط)

### M0 — التأسيس والهوية
- [x] **F0.1** (كومِت 162aec4e — 2026-09-11) مصفوفة التتبع التفصيلية: مسح قرارات الخطة ١–٤٧ واحداً واحداً وتعيين Task ID لكل قرار في §١١ (مخرَج إلزامي قبل الخروج من M0)
- [x] **F0.2** إنشاء `app/` (Flutter) + تثبيت الحزم §٢ + تثبيت الإصدارات في pubspec + بناء أول APK فارغ
  - **قرار ٥١ — البناء المحلي:** بيئة الجلسة لا تملك Flutter SDK وpub.dev محجوب عنها ⇒ **الكود يُكتب كاملاً في الجلسة، والبناء على جهاز صاحب المشروع (ويندوز/cmd.exe)**. حلقة العمل: أكتب ← صاحب المشروع يشغّل `flutter pub get` ثم `flutter build apk --debug` ← يلصق المخرجات **حرفياً** (نجاح أو نص الخطأ كاملاً) ← أصلح ← يعيد. تعريف الإنجاز: أول APK يُبنى بنجاح على جهازه + تثبيته يفتح شاشة فارغة بهوية docs/13. تجهيز جهازه (مرة واحدة): تنصيب Android Studio + Flutter SDK stable ثم `flutter doctor` وتلصيق نتيجته هنا قبل أول بناء. تنبيه: اختيار إصدارات الحزم يتم بالمطابقة الموثقة (بحث نسخ pub.dev) لأن الجلسة لا تستطيع تجربة الحل محلياً
- [ ] **F0.3** تطبيق رموز docs/13 في `core/theme/` (داكن/فاتح، الخطوط Cairo+Tajawal، قاعدة CTA داكن-على-أخضر) — widget test تباين لكل زوج نص/سطح
- [ ] **F0.4** CI على GitHub Actions: `flutter analyze` + `flutter test` + بناء APK على كل دفع (توثيق أي قيود token — بدون workflows scope يُبنى محلياً بأمر موثّق)

### M1 — النواة الحتمية (docs/12 → Dart)
- [x] **F1.1** `core/rng/splitmix64.dart` بدقة 64بت أصلية + نسخة BigInt مرجعية للاختبار
- [x] **F1.2** `core/rng/session.dart`: buildSession بالتيارات الثلاثة + Fisher–Yates + توازن فصول round-robin
- [x] **F1.3** اختبارات golden vectors: تطابق بتّي مع مرجع JS/BigInt (بذور `0`,`1`,`2^64−1`,`0x1234567890ABCDEF`) + معايير قبول docs/12 §٢.٤ (تفرد/توازن/عدالة 24–26٪)
- [x] **F1.4** `core/fsrs/fsrs5.dart` + أوزان W الافتراضية + اختبار محاكاة ١٠ مراجعات بفترات متوقعة معلومة

### M2 — خط المحتوى
- [ ] **F2.1** مخطط JSON للحزمة (وحدات/فصول/فقرات/خلاصات/أسئلة/بطاقات/تجارب) + محول من مخرجات أدوات tools القائمة
- [x] **F2.2** تحميل المحتوى من assets + فحص سلامة (hash لكل ملف) + إصدار المحتوى
- [ ] **F2.3** بوابة المعادلات: flutter_math_fork على S1–S4 الفعلية (الأجهزة الأربعة) — قرار اعتماد/بديل موثّق
- [ ] **F2.4** آلية «تجميد المعدَّل»: أي بند يحتاج مراجعة الأستاذ يُستبعد من الحزمة آلياً

### M3 — التطبيق المحلي الكامل (يعمل 100% أوف لاين)
- [ ] **F3.1** مخطط Drift كامل (منهاج/تقدم/أخطائي/بطاقات+حالة FSRS/دفتر XP/سلسلة/إعدادات) + تشفير SQLCipher
- [ ] **F3.2** شاشات المنهاج: قراءة فقرة/شاشة + «التالي» + خلاصة الفقرة + TTS «اسمعني» (إخفاء عند غياب المحرك العربي) + فهرس قابل للطي + «انتهى الدرس» (مطابقة النموذج)
- [x] **F3.3** التدريب: دفعات ١٠/١٠ بالبذرة اليومية (`mix64(deviceId⊕toDays)` docs/12 §٢.١ — متجه ذهبي مختبر) + شاشة سؤال/خيارات بتصحيح فوري وخطوات الحل + نتيجة نهاية الدفعة + أرشيف أخطائي (الأحدث محل الأقدم) + استئناف منتصف الدفعة + **بوابة القرار ٢٤**: بلا معتمد ⇒ شاشة الانتظار (الواقع حتى إقرار الأستاذ). التلميحات ٣ الأوضاع ودرجات الحل الجزئي (مُعيارا ٩/١٠): عند فتح البنك — بنك MCQ الحالي ثنائي التقييم
- [ ] **F3.4** البطاقات: طابور اليوم (سقف ٢٠ + ٦ جديدة) + بطاقة كشف/استرجاع + ٣ أزرار تقييم → FSRS + شاشة الإتمام (+١٥)
- [ ] **F3.5** المختبر: محاكاة النابض RK4 (CustomPainter) بمنهجية PhET/POE كاملة + التحدّي T=٢ث — ثم بقية التجارب الخمس بنفس القالب
- [ ] **F3.6** التفعيل: بوابة أول فتح + تنسيق الكود + فحص Ed25519 محلي ظاهر + انتظار تدريجي ٥ محاولات + وضع التجربة + إدارة داخل حسابي (مطابقة قرار ٤٤ والنموذج)
- [ ] **F3.7** دفتر XP المحلي: أحداث docs/12 §٤.١ + سلسلة هاش SHA-256 + توقيع ed25519_edwards بمفتاح الجهاز (secure_storage) + مهام اليوم + نقاط يدوي
- [ ] **F3.8** السلسلة اليومية (نافذة ٤فجر→٤فجر) + الرئيسية (ترحيب/واصل/بطاقات اليوم/فكرة اليوم)

### M4 — Supabase والمزامنة
- [ ] **F4.1** مشروع Supabase (**قرار ٥٠: الخطة المجانية رسمياً**) + بيئة إنتاج واحدة (staging = مشروع مجاني ثانٍ إن لزم) + إدارة مفاتيح (service_role خارج العميل أبداً) + **ping مجدول إلزامي** (GitHub Actions أسبوعي أو UptimeRobot — تفادي توقف الخمول ٧ أيام §١٥-د) + **سياسة تقاعد أحداث XP**: الاحتفاظ بأحداث الأسبوع الحالي + مجاميع الأسابيع السابقة (القاعدة < 100MB دائماً)
- [ ] **F4.2** الترحيلات §٥ كاملة + RLS على كل جدول (اختبار anon/authenticated سالب وإيجابي)
- [ ] **F4.3** Auth: دخول مجهول عند التفعيل + ربط profile/device (بصمة الجهاز)
- [ ] **F4.4** `license_activate` + سجل licenses من أداة المكتب (F6.1) + إلغاء/تجديد
- [ ] **F4.5** محرك المزامنة: طابور العمليات → `verify_xp_events` (فحص seq/hashtree/توقيع الجهاز المسجل) → إيداع الدوري؛ المشغّل **الأساسي أمامي** (فتح التطبيق/عودة الاتصال — OEMs شاومي/هواوي/OPPO تقتل الخلفية [§١٥-أ]) + WorkManager جهد إضافي فقط
- [ ] **F4.6** `league_rollup` الأسبوعي (تجميعات ~٣٠ بمستوى متقارب) + شاشة دوري فيزيا كلاش (بنفسجي docs/13) + موسم ينتهي قبل الامتحان بشهر

### M5 — التحديات الحية
- [ ] **F5.1** `duel_finish` السيرفر-المرجعي: إعادة توليد الجلسة من البذرة وتصحيح الإجابات دونه — أي تعارض يرفض النتيجة
- [ ] **F5.2** قنوات خاصة `duel:{id}`: Presence (لوبي) + Broadcast (إجابات <50ms) + `duel_access` RLS
- [ ] **F5.3** تدفق المبارزة الكامل بالواجهات (إنشاء→لوبي→لعب بشريطي تقدم وسلسلة ×٢/×٣→نتيجة+ثأر+مشاركة) — مطابقة النموذج وقرار ٤١
- [ ] **F5.4** النقلية المحلية: nearby_connections ببوابة فحص GMS (هواوي HMS بلا GMS ⇒ البديل فوراً)؛ fallback Hotspot قرار ٢٥ + تحدي QR بالبذرة — مبدّل النقلية التلقائي
- [ ] **F5.5** اختبار عادل: نفس البذرة على جهازين فعليين ⇒ نفس الأسئلة والنتائج عبر النقليات الثلاث

### M6 — الأدوات الإدارية
- [ ] **F6.1** أداة المكتب (ويب محلية/CLI): إصدار أكواد موقّعة + طباعة + إلغاء — POS قرار ٣٥
- [ ] **F6.2** لوحة الأستاذ: مراجعة المحتوى (حفظ تلقائي/استئناف/فلترة/زر واتساب قرار ١٩) مرتبطة بخط F2 — منصتها: ويب مستضاف مع OTP بريد للأستاذ (تُحدَّد نهائياً عند بدء F6.2)
- [ ] **F6.3** الشهادات: `cert_issue` نهاية موسم (🥇سنتان/🥈سنة + المرونة قرار ٤٠ + شهادة موقعة بشعار loraneem-tech)
- [ ] **F6.4** صفحة «عن التطبيق» النهائية (شارة الأستاذ + المطور + الإصدار) — عند توفر الشعار يُستبدل النصي

### M7 — الإتقان وقبل الإطلاق
- [ ] **F7.1** مصفوفة أجهزة S1–S4: أداء المحاكاة، TTS، المعادلات، حد Android الأدنى نهائي + **سحب إجهاد PRNG ‏10^6 جلسة (منقول من F1.3 — قرار ٥٢)** — تقرير موثّق
- [ ] **F7.2** تدقيق تباين docs/13 على كل الشاشات (أداة قياس وثيقة الهوية) + RTL كامل + أحجام خط النظام
- [ ] **F7.3** أداء: بدء بارد <2.5ث، بلا jank بالقوائم، استهلاك بطارية مزامنة مقبول
- [ ] **F7.4** أمان: لا أسرار بالحزمة، RLS سالبة، حدّ المحاولات، كشف عبث الساعة (آخر وقت مُرى)، تدقيق قنوات خاصة

### M8 — الإطلاق
- [ ] **F8.1** حزمة `com.loraneemtech.fizyaclash` + أيقونة/شعار نهائي + لقطات + سياسة خصوصية (اسم المتجر: فيزيا كلاش — Clash of Physics)
- [ ] **F8.2** تجريبي مغلق (طلاب مختارون) ← إصلاحات ← إنتاج — **التوزيع المباشر APK من المكتب هو القناة الأساسية** (Play Console مقيد لسوريا حتى فبراير 2026 [§١٥-ج]) وPlay فرصة عند انفتاحه
- [ ] **F8.3** خطة تشغيل: نسخ احتياطي Supabase + مراقبة logs + قناة دعم (مكتبكم)
- [ ] **F8.4** تحديث ذاتي للـAPK (**قبل التجريبي المغلق**): مقارنة النسخة بجدول `app_versions` ← تنزيل ← تثبيت — لأن التوزيع المباشر بلا متجر

### M9 — ما بعد v1 (مؤجل بقرار، لا يفتح قبل F8.3)
- [ ] **F9.1** معايرة FSRS الشخصية (~1000 مراجعة) · **F9.2** تجميد السلسلة كعملة · **F9.3** أرشيف «أبطال المواسم» · **F9.4** OCR امتحانات 2013–2017

## ٧. معايير القبول النهائية لـv1 (بوابة F8.2)
1. كل مهام M0–M7 مشطوبة ومحققة، وصفر معوّق مفتوح.
2. التطبيق يعمل **كاملاً** بلا شبكة (طيران كامل ٤٨سا: دراسة/تدريب/بطاقات/مختبر/تفعيل جديد/نقاط).
3. مبارزة حية عبر النقليات الثلاث ببذرة واحدة = نتائج مطابقة (F5.5).
4. دفتر XP: أي تلاعب مُفترض يُرفض 100٪ في اختبارات F4.5.
5. تباين AA مقيس على كل شاشة، وضع فاتح وداكن.

## ٨. المخاطر وتخفيفها
| خطر | تخفيف |
|---|---|
| صيانة nearby_connections | تحقق F5.4 + بديل Hotspot (قرار ٢٥) جاهز |
| معادلات على أجهزة ضعيفة | بوابة F2.3 على S1–S4 + بديل صور مولّدة |
| limits Realtime الحرة (200) | كافية للانطلاق؛ مراقبة وترقية Pro عند النمو [5] |
| تعارض مزامنة | LWW + معاملات Drift + سجل تعارضات للفحص |
| استعادة بيئة Git (حدث مرتين) | وصفة §١٠ الملحق + فحص ls-remote بعد كل دفع |
| اعتماد على CDN للخطوط | ممنوع — الخطوط داخل assets (docs/13) |
| OEM يقتل المزامنة الخلفية (MIUI/هواوي) | المشغّل الأمامي أساس (F4.5) + إرشاد استثناء المستخدم |
| توقف مشروع Supabase المجاني (خمول ٧ أيام) | **حُلّت بقرار ٥٠**: البقاء بالمجاني + ping مجدول إلزامي + تقاعد أحداث XP |
| ed25519_edwards متجمّدة | الحزمة `cryptography` النشطة أساس + متجهات تكافؤ F1.3 |

## ٩. حوكمة التغيير
أي تعديل على هذه الخطة (إضافة مهمة/تغيير ترتيب/تقنية جديدة) = **قرار جديد برقم في 01-PLAN** مع بحثه إن كانت ميزة — ثم ينعكس هنا في §٦ و§١١ فوراً.

## ١٠. ملحق — قواعد تشغيلية دائمة (من قرارات الجلسات)
- cmd.exe فقط على ويندوز صاحب المشروع (`%USERPROFILE%`, `chcp 65001`) — لا PowerShell.
- دفع HTTP 408 متكرر: إعادة ثم `git config http.postBuffer 524288000`.
- تعديلات نفس الملف تسلسلية حصراً + grep تحقق بعد كل دفعة.
- استعادة بيئة Git: `git fetch origin '+refs/heads/*:refs/remotes/origin/*'` → `git reset --soft origin/arena/...` → `git add -A` → تحقق diff ثم كومِت جديد.
- لا ملفات مولّدة (build/, __pycache__) داخل Git — `.gitignore` يُحدَّث فوراً.
- **حلقة البناء المحلي (قرار ٥١):** مخرجات `flutter` تُلصق حرفياً دوماً — لا تلخيص ولا «ما زبطت» — والخطأ يُصلَح من نصه الكامل. `flutter doctor` نظيف شرط قبل أول بناء.

## ١١. مصفوفة التتبع التفصيلية — القرارات ١–٥١ ⇒ المهام (أُنتجت في F0.1 — مصدر الحقيقة)

> القاعدة: لا يُعلَن اكتمال مرحلة قبل اكتمال مهام كل قراراتها. «تحقق» = الدليل المطلوب عند شطب المهمة.

| القرار | مختصر | تهبط في | التحقق |
|---|---|---|---|
| ١ | منهاج بنسخة «سنة+طبعة» | F2.1 | حقول year/print في المخطط واختبارها |
| ٢ | خمس وحدات معتمدة | F2.1 + F3.2 | شجرة الوحدات تطابق terms_data |
| ٣ | توزيع محلي APK | F8.1 + F8.2 | قناة التوزيع الموثقة |
| ٤ | أوف لاين بالكامل | M3 كاملة + F7.1 | اختبار طيران ٤٨سا (§٧.٢) |
| ٥ | امتحانات+سلالم = مصدر ذهبي | F2.1 | بنية بنك مستخلصة من السلالم |
| ٦ | فرق المنهاج سنوياً | F2.2 | عملية تحديث نسخة موثقة |
| ٧ | توليد صفر-خطأ + مراجعة إلزامية | F2.4 + F6.2 | أي بند غير مراجَع محجوب |
| ٨ | اقتصاديات الاشتراك | F6.1 | نص الكود/الصلاحية متسق مع ٢٧/٤٠ |
| ٩ | تلميحات ٣ أوضاع | F3.3 | إعداد التلميح يعمل |
| ١٠ | solution_steps + follow_through | F2.1 + F3.3 | درجات جزئية مطابقة السلم |
| ١١ | ExamBlueprint وزن وزاري | F2.1 + F3.3 | توزيع الدفعات حسب المخطط |
| ١٢ | محاكاة لا فلاش | F3.5 | المحاكاة التفاعلية تعمل |
| ١٣ | مخزون مسبق + «ولّد المزيد» | F2.2 + F3.3 | بدون انتظار شبكة |
| ١٤ | رموز الكتاب حرفياً | F2.1 (TermLinter) | صفر انحراف على الحزمة |
| ١٥ | ألفاظ رسمي/SYN/BAN | F2.1 + F2.4 | Linter يمنع BAN |
| ١٦ | TermLinter مقابل القاموس النهائي | F2.1 | يشغَّل في خط المحتوى |
| ١٧ | المُلتبس يُعلَّق ويُسأل | دستور + F6.2 | سلوك الجلسات + سجل المعلّقات |
| ١٨ | مراجعة تبدأ فارغة | F6.2 | لا ختم مسبق في الواجهة |
| ١٩ | أداة مراجعة لمعلّم غير تقني | F6.2 | حفظ/استئناف/فلترة/واتساب |
| ٢٠ | إيجار ٣٠ يوماً | F3.6 + F4.4 | إعادة التحقق الدورية |
| ٢١ | ملف الأستاذ مصدر مؤقت | F2.1 | موسومة كمصدر مرجعي |
| ٢٢ | بذرة حتمية أوف لاين | F1.1–F1.3 + F5.5 | golden vectors + اختبار العدالة |
| ٢٣ | تبادل نتائج QR | F5.4 | تبادل كامل بلا شبكة |
| ٢٤ | أسئلة التحدي من المعتمد حصراً | F1.2 + F2.4 | فلترة status في buildSession |
| ٢٥ | توقيع النتائج بمفتاح التفعيل | F3.7 + F5.4 | توقيع/تحقق النتيجة المحلية |
| ٢٦ | تفعيل أونلاين مرة + حزمة تجريبية | F3.6 + F4.4 | مسار التجربة بلا كود |
| ٢٧ | إيجار Ed25519 ≤٣٠ي + hard_deadline | F3.6 + F4.4 | فحص محلي بالعقد الموقّع |
| ٢٨ | جهازان + تهدئة ٧ أيام | F4.4 | سيناريو نقل الاشتراك |
| ٢٩ | زمن موثوق + كشف رجوع الساعة | F3.6 + F7.4 | اختبار عبث الساعة يُرفض |
| ٣٠ | تشفير المحتوى AES-GCM/sealed | F2.2 + F3.1 | فتح الحزمة بمفتاح الجهاز |
| ٣١ | إشارات لينة (روت/توقيع) | F7.4 | أثر على الثقة فقط |
| ٣٢ | علامة مائية + playbook | F6.1 + F7.4 | موزّع مرمَّز بالكود |
| ٣٣ | Supabase بنية + RLS | M4 كاملة | اختبارات RLS سالبة/إيجابية |
| ٣٤ | POS مكتب مباشرة | F6.1 | إصدار كود موقّع كامل الدورة |
| ٣٥ | تحديات هجينة ثلاثية | F5.2–F5.5 | النقليات الثلاث ببذرة واحدة |
| ٣٦ | دوري موسمي قبل الامتحان بشهر | F4.6 + F6.3 | نافذة الموسم والشهادة |
| ٣٧ | تجارب داخل الدروس + فهرس مختبر | F3.5 + F3.2 | التجربة بموقعها في الدرس |
| ٣٨ | تفعيل أول فتح + مدخل تجريبي | F3.6 | مطابقة النموذج |
| ٣٩ | عن التطبيق (شارة+مطور) | F6.4 | مطابقة النموذج |
| ٤٠ | جوائز + تنويه المرونة حرفياً | F6.3 | نص قرار ٤٠ حرفي بالشهادة |
| ٤١ | بحث أولاً + نقاط دوماً + دوري | دستور + F3.7 + F4.5/F4.6 | دفتر XP يعمل بلا تحدٍّ |
| ٤٢ | ملخصات SRS كشف واسترجاع | F3.4 | FSRS يضع الطوابير |
| ٤٣ | مختبر PhET/POE | F3.5 | المنهجية كاملة بالتجربة |
| ٤٤ | تفعيل بوابة + إدارة بحسابي | F3.6 | مطابقة النموذج |
| ٤٥ | مستند الخوارزمية | M1 كاملة | F1.3 vectors |
| ٤٦ | فيزيا كلاش — بلا «دمشق» | F0.3 + F8.1 | مراجعة نصوص كل الشاشات والمتجر |
| ٤٧ | الهوية البصرية المقيسة | F0.3 + F7.2 | widget-test تباين + تدقيق شاشات |
| ٤٨ | خطة التنفيذ + دستور الجلسات | 00-CONSTITUTION + docs/14 | التزام البروتوكولات |
| ٤٩ | غير المرفوع غير منجز + تدقيق §١٥ | دستور §٥ + §١٥ | ls-remote بعد كل دفع |
| ٥٠ | Supabase المجاني + ping + تقاعد | F4.1 | عمل الـping وسياسة التقاعد |
| ٥١ | جهاز صاحب المشروع مصنع البناء | F0.2 + §١٠ | أول APK يُبنى على جهازه |

## ١٢. خريطة التقدم (الحقيقة اللحظية — تُحدَّث في نهاية كل محادثة)
> الحالة المبدئية: كل المهام `[ ]` — الشطب يحدث هنا فقط مع رقم الكومِت.

## ١٣. الحالة الحالية
- **أُنجز حتى الآن:** قرارات ١–٥٢ + F0.1/F0.2 + M1 محرك كامل (F1.1/F1.2/F1.3) — **١٢/١٢ اختباراً ناجحاً** على جهاز صاحب المشروع (شاومي Android 16) + F1.4 FSRS (كود + اختبارات مرجعية).
- **التالي:** F3.2 جزء أول **أخضر 35/35 على جهاز المالك** (cb1192b8 + إصلاحان d498975 Expanded للأزرار وf2b2f57 حرب Material حول ListTile الفهرس — علّتان انكشفتا بالتتابع لأن الأولى كانت تُغرق الاختبار قبل الثانية) - **قرار ٥٦ (اختيار المالك «ب»):** الطفو العام على نفس المستودع مع بقاء التاريخ — تقييم الخطر: منخفض (النصوص موزعة رسمياً وبلا مقابل من وزارة التعليم عبر منصة فداء وهي مصدرنا)؛ المسح قبل الطفو: صفر مفاتيح/أسرار بالشجرة (فحص jks/keystore/key.properties/env). قلب الطفو بيد المالك حصراً (صلاحيات التكامل 403 — سليم أمنياً).
- **قرار ٥٥ (تنظيف ما قبل الطفو العام):** سحب `sources/fadaa` من التتبع + .gitignore دائم — نصوص الكتاب تبقى بشجرة عمل الوكيل محلياً للتأليف ولا تُدفع بعد اليوم.
- **TTS «اسمعني» مرفوع أيضاً (677a9b79 — واجهة Speaker + إخفاء بلا محرك عربي) والمجموع 37 اختباراً بانتظار جولة المالك**؛ ثم F3.1 (Drift).
- **التأليف الرسمي بدأ (قرار ٥٣):** U1C1 «النواس المرن غير المتخامد» مؤلَّف من نص فداء (٦ فقرات + ٤ أسئلة محجوبة approved:false + بطاقتان) — بفحص BAN مسبق صفر ضربات — وسجل الثبات في docs/15-AUTHORING-LEDGER.md. ثم U1C2 «التوابع الزمنية» (٦ فقرات + ٣ أسئلة + ٣ بطاقات — فحص BAN صفر) — الاختبارات صارت **38**. **الوحدة الأولى «النواسات» مكتملة**: ٨ فصول، ٣٦ فقرة، ٢٨ سؤالاً، ١٩ بطاقة. **الوحدة الثانية «الأمواج المستقرة» مكتملة التأليف**: U2C1–U2C5 (عرضية/أوتار/كهرطيسية/طولية/مزمار) — الحزمة: **١٣ فصلاً، ٥٦ فقرة، ٤٤ سؤالاً، ٢٩ بطاقة**. ### ✦ ختم التأليف (2026-09-12): **الوحدات الخمس مكتملة** — الحزمة النهائية: **24 فصلاً، 95 فقرة، 71 سؤالاً محجوباً، 49 بطاقة** — من نصوص فداء حصراً، BAN صفر، وكل سجل الثبات في docs/15 (§٧ ختم التأليف). المرحلة التالية: مراجعة الأستاذ فقرةً فقرة ثم فتح الأسئلة بقراره. **F3.1 بدأ (قرار ٥٧):** التقدم الحقيقي — واجهة ProgressStore + InMemory + SharedPrefs (KV يكفي لتقدم القراءة؛ الـDrift يُؤجل لدفتر XP في F7 حيث تلزم المعاملات — والواجهة تجعل الترحيل رخيصاً) — نسبة الوحدات حقيقية بدل ٠٪ + علامة ✓ للفصول المكتملة + تحديث عند العودة، **وجزء ثانٍ: الاستئناف** — الدرس يفتح من موضع القارئ المحفوظ وزر «متابعة القراءة» بالوحدة (ويصلح «ابدأ القراءة» بعد الإتمام) — الاختبارات **40**. **F3.3 التدريب مكتملة (قرار ٥٨):** البذرة اليومية فوق محرك M1 (SplitMix64 + توازن الفصول + خلط خيارات حتمي) + جلسة سؤال/خيارات + نتيجة + أرشيف أخطائي + استئناف منتصف الدفعة — البنك مقفول بقرار ٢٤ حتى مصادقة الأستاذ (الجهاز يرى شاشة الانتظار). علّتا جولة المالك أصلحتا: const {} بلا نوع يستنتَج Set فيكسر الترجمة (LUB=Object — النوع الصريح <String,dynamic> إلزامي) + فهرس فقرة برنولي [1] لا [2]. العدد الكلي بعد F3.3: **55** (46 غير-واجهة + 9 واجهة — جرد آلي). **تصحيح جوهري لقرار ٥٩ بلصقة تحليل المالك:** الجذر ليس الـinfos (هي لا تفشل البوابة) — **أداة flutter ببيئة البوت زائفة (4.0.0) لا تحلل شيئاً**، فكان الاحمرار المزمن **أخطاء حقيقية عديمة الرؤية عنّا**: for-in فوق Map بلا .entries + توقيع buildDailyBatch مسمّ والنداءات موضعية (٢٧ خطأ دفعة واحدة). البوابة تستحقه بعيون المالك أو CI حصراً — القرار ٥٤ يمنع الاعتماد على تحليل البيئة أصلاً. جولة المالك الثانية: ٤ متبقية (ByteData/Uint8List يتيمان بعد إزالة flutter/services — dart:typed_data هو الصحيح) أصلحتها. وبعد خضراء التحليل أول مرة: **الاختبارات عملت حقيقياً لأول مرة** — 51 نجح و4 واجهة سقطت (أسماؤها محجوبة — السجلات مسدودة) + بق UTC أصلحته: dayNumberOf كان DateTime محلياً فينزيح بالمناطق +3 — DateTime.utc إلزامي (docs/12: أيام الإيبخ). جولة المالك الثالثة: **التحليل نظيف عند المالك («No issues found!»)** + تفكيك الـ4 الساقطة: (١) طومسون — المتوقع فقرة بسيطة والنص بسلسلة الاشتقاق «T0 = 2π/ω0 = 2π·√(L·C)» (كانت مقنّعة بعد برنولي كالسابق) (٢) المتتبَّع هو معرف الفصل U1C1 لا الوحدة (٣) **بقّ حقيقي: _saveCursor كانت مفقودة كلياً** — cursor يُكتب عند الإتمام حصراً فلا «متابعة القراءة» أبداً — أُضيفت ووُربطت بالتالي والسابق (٤) اختبار الدورة صُلّد بطبقة المخزن (إثبات وصول الإجابة) والنقر عبر بلاطة ListTile وإطار إضافي. **تقنية التفتيت التشخيصي (أسماء CI محجوبة):** الاختباران الملتصقان قُسّما إلى 8 اختبارات فرعية مستقلة (أ-د لكل منهما) — عدّادات CI تكشف أي خطوة تنكسر بدقة. **تأكيد حاسم بالصدفة:** رقعة اختبارية فقط أعادت إحمرار «تحليل ساكن» (مساعدان محليان بشرطة سفلية أطلقا lint) ⇒ **flutter analyze = fatal-infos مؤكد ×2: البوابة تسقط على أي ملاحظة مهما صغرت**. **⚠️ نسخة ci-workflow-backup.yml (v2) فُقدت من بيئة البوت** — يحتاج المالك إعادة لصقها. بوابة الألفاظ أوقفت مرتين موثقتين: «الكثافة» و«التيار المستمر» — استبدلتا بالرسمي قبل الكتابة. تصحيح موثق: ملف الكهرباء = دارة الاهتزاز والتيار المتناوب لا أوم/توصيل. (ملاحظة: تحديث هذا السطر تأخر كومِتاً عن الحقيقة — الحزمة فعلياً كما هو مذكور).تصحيح موثق: ملف الأمواج = أمواج مستقرة (أوتار/كهرطيسية/طولية/مزامير) لا سونار/دوبلر. ⚠️ حادثة موثقة: تعديلان متوازيان على ملف الاختبار أسقطا عداداً بصمت — القاعدة: تعديلات الملف الواحد تسلسلية حصراً. الوحدة الأولى النواسات بات اقتراب اكتمالها (يتبقى: الثقالة، الالتواء، الأخطاء الشائعة). وF0.3 معلقة على ملفات الخطوط.
- **pack.json على الجهاز (17d228a7):** سبب «unable to load asset» كان غياب الملف كلياً — الاختبارات بالحقن لا تلمسه. النسخة الحالية = عرض (فكستشر U1 + U2–U5 هيكل فارغ بأرقام ترتيبية)؛ التأليف الرسمي الكامل مهمة مستقلة قادمة.
- **معوّقات:** شعار loraneem-tech (بانتظار رفع صاحب المشروع — لا يسدّ M0–M3).
- **تدقيق تنفيذي مكتمل:** 2026-09-11 — النتائج والتعديلات في §١٥ (قرار ٤٩).

## ١٤. المعوّقات (سجل حي — يُمسح عند الحل)
- **F0.3:** بانتظار ملفات الخطوط Cairo+Tajawal (الروابط في app/BUILD.md §الخطوط) من صاحب المشروع.

---

## ١٥. التدقيق التنفيذي المعمّق (2026-09-11 — قرار ٤٩)

> تدقيق ما قبل البناء طلب صاحب المشروع: «افحص خطتنا بعد بحث عميق عن أي مشكلة تنفيذية» — كل بند: المشكلة ← الدليل ← المعالجة (المهام المعدلة أعلاه تعكسها).

| # | المشكلة التنفيذية | الدليل | المعالجة |
|---|---|---|---|
| أ | **أجهزة السوق تقتل المزامنة الخلفية**: شاومي/هواوي/OPPO (غالبية طلابنا) تؤخر WorkManager ساعات أو توقفه كلياً — «Production Android ≠ Google Android» | [Beyond Doze — OEM Realities 2026](https://proandroiddev.com/beyond-doze-building-reliable-background-execution-on-modern-android-including-oem-realities-5fa0a6e05672) + [SO — Chinese ROMs](https://stackoverflow.com/questions/59906497/work-manager-on-chinese-roms-like-xiaomi-and-oppo-when-under-battery-optimizati) | F4.5: المشغّل الأمامي أساس (فتح التطبيق/عودة الاتصال) — WorkManager إضافي فقط + إرشاد استثناء بطارية اختياري |
| ب | **Nearby يحتاج GMS**: هواوي HMS بلا خدمات غوغل لا تشغّله؛ وحتى أجهزة GMS تعطّلت بتحديثات Play Services | [SO — Huawei no GMS](https://stackoverflow.com/questions/64970650/error-compiling-my-application-flutter-application-on-a-huawei-p40-lite-without) + [SO — nearby breakage](https://stackoverflow.com/questions/77120208/flutter-nearby-connections) | F5.4: بوابة فحص GMS قبل التفعيل — عدم التوفر ⇒ Hotspot/QR (قرار ٢٥) مباشرة |
| ج | **Play Console ما زال مقيداً لسوريا**: فشل تسجيل مطور (الدفع + غياب سوريا من قائمة الدول) حتى فبراير 2026 — رغم عودة Play Store تدريجياً منذ يناير 2026 بعد رفع العقوبات (أغسطس 2025) | [unblocksyria — Play Console](https://unblocksyria.com/en/services/google-play-console) + [Daily Sabah — عودة الخدمات](https://www.dailysabah.com/business/tech/google-services-start-resuming-in-syria-after-years-long-ban) + [ClefinCode — رفع العقوبات](https://clefincode.com/blog/global-digital-vibes/en/from-isolation-to-integration-sanctions-lifting-on-syria-the-global-rise-of-syrian-expertiseandmarketpotential) | F8.2: التوزيع المباشر APK قناة أساسية (يتكامل مع POS المكتب أصلاً) + **F8.4 جديدة**: تحديث ذاتي داخل التطبيق + مراقبة انفتاح Console |
| د | **مشروع Supabase المجاني يتوقف** بعد ٧ أيام خمول قاعدة حتى ضغط Resume يدوياً — إجازة صيفية صامتة = دوري معطّل | [Layerbase — pause policy](https://layerbase.com/blog/sleep-vs-pause-vs-archive) + [ITPath — keep-alive options](https://www.itpathsolutions.com/supabase-free-tier-limits) | F4.1: ping مجدول (Edge Function مجدولة) أو ترقية Pro ‏$25/شهر — قرار ميزانية لصاحب المشروع عند بدء M4 |
| هـ | **حزمة ed25519_edwards متجمّدة** (آخر إصدار 0.3.1 — سبتمبر 2021) خطر صيانة على ميزة أمنية حاكمة | [pub.dev — السجل](https://pub.dev/packages/ed25519_edwards/versions) | الأساسية صارت `cryptography` النشطة؛ الاحتياطية تبقى؛ F1.3 يوسّع المتجهات لتشمل تكافؤ التواقيع بين الحزمتين |
| و | **جهازان لكل كود + LWW**: مراجعات بطاقات متزامنة من الجهازين قد تفقد إحداها (آخر كتابة يغلب) | تحليل داخلي — نمط معروف بالأوف لاين أولاً [flutterstudio](https://flutterstudio.dev/blog/offline-first-flutter-drift.html) | v1 مقبولة (الغالبية جهاز واحد)؛ v1.1: سجل مراجعات append-only يُعاد حساب FSRS منه — مرشحة في M9 |
| ز | **تنبيهات السلسلة غائبة عن الخطة** (حلقة الاحتفاظ الأقوى عند Duolingo) | تحليل تغطية المهام | مقترحة v1.1: إشعارات محلية (flutter_local_notifications + AlarmManager تقريبي — بلا FCM لضمان الأوف لاين) — تُعتمد بقرار جديد حينها |

**الخلاصة:** لا مشكلة تُلغي الخطة — ٤ تعديلات مهمة (F4.5، F5.4، F8.2، F8.4). ميزانية Supabase **حُسمت بالمجاني** (قرار ٥٠)؛ يبقى تنبيهات السلسلة مرشحة v1.1. الخطة جاهزة للتنفيذ.

## ١٦. بحث معمّق: اقتصاد النقاط وسلامتها — منع الاحتيال والتكرار (قرار ٥٣ — يَحكُم F6/F7 قبل كتابة أي سطر منه)
> قرار ٥٣ (المالك): إكمال التطبيق كاملاً وفق خريطة M3→M9 — والنقاط ببحث موثق هذا قبل تنفيذها.

### أ. أساليب الغش المرصودة (دوبولينجو حالة دراسية مثالية — نفس بنيتنا: XP + دوري أسبوعي)
- **XP farming:** تكرار الأنشطة الرخيصة عالية النقاط؛ استغلال الجولات المؤقتة.
- **بوتات وسكربتات إكمال آلي** (أشهرها DuoHacker v4 — إكمال كورس كامل آلياً)؛ auto-clickers.
- **مولدات XP وهمية** = احتيال بالمستخدم نفسه (تصيد) — يثبت أن الطلب موجود.
- **حسابات مشتركة/متعددة** لتضخيم الدوري؛ تلاعب تنافسي (إخفاء التقدم عن المنافسين).
- **سباق تسلح مجتمعي:** الشك بالآخرين يولّد غشاً مضاداً — العدالة المرئية مطلب تصميمي لا رفاهية.
- روابط: duolingoguides.com/duolingo-leagues-cheating/ · kotaku.com/duolingo-app-cheats-hacks-leagues-xp-why-duohacker-1850506482 · aiexamhelper.com/blogs/duolingo-cheats/ · linksiwouldgchatyou.substack.com/p/porque-no-tampoco

### ب. التوقيع ≠ الطزاجة — CWE-294 (Capture-Replay)
- التوقيع يثبت **المصدر والسلامة**، لا «متى» — طلب موقّع بلا طابع زمني داخل النص الموقَّع يُعاد إلى الأبد.
- **الطابع الزمني يجب أن يكون داخل النص الموقَّع** (ترويسة خارجه تُعدّ بحرية).
- **Nonce مراقَب خادمياً** داخل النافذة + كاش معرّفات معالَجة (TTL) يمنع التكرار داخل النافذة.
- تصنيف الحدودية: Tier 2 (نقاط/هوية) = نافذة ضيقة + دلالة استعمال-مرة.
- روابط: dev.to/roxdavirox/hmac-proves-origin-not-freshness-replay-attacks-against-signed-apis-1e53 · technori.com/news/protect-apis-from-replay-attacks/ · dev.to/havenmessenger/verifying-webhooks-the-four-checks-handlers-skip-29gp · webflow.com/blog/webhook-security

### ج. عدم تماثل المفاتيح يتفوق على السر المشترك
- Ed25519/ECDSA: مفتاح التحقق **علني** — تسربه لا يزوّر شيئاً؛ HMAC: تسرب السر = تزوير كامل.
- ⇒ تأكيد مباشر لاختيار docs/12: دفتر XP موقّع بجهاز الطالب (Ed25519) والتحقق الخادمي بمفتاح علني (verify_xp_events).

### د. مفاتيح الإسناد (Idempotency) — حجر الأساس ضد التكرار
- كل حدث XP بمعرّف UUID فريد يولّده المصدر؛ الخادم: `UNIQUE(event_id)` + `ON CONFLICT DO NOTHING`.
- إعادة الإرسال (شبكة متقطعة/إعادة تشغيل/هجوم إعادة) تعيد **النتيجة نفسها** بلا ازدواج — يحمي الواقع السوري المتقطع والفاقد المتعمد معاً.

### هـ. فحوص السرعة (Velocity) — عتبات بلا قتل التحويل
- العتبة من بيانات سلوك مشروع حقيقي: **فوق مئين ٩٩** — تصطاد ١٪ الشاذة دون معاقبة ٩٩٪.
- **نوافذ متدرجة** (دقيقة/ساعة/يوم): كل نافذة تصطاد نمطاً مختلفاً (آلي فوري/جلسة منسقة/اختبار مستمر).
- **ترجيح بالتاريخ:** حساب جديد بقفزة عالية أشد شكاً من حساب بعشرات الجلسات.
- **مزدوج:** حظر آلي للقصوى الصارخة فقط؛ الحدودية → طابور مراجعة بشرية (الأستاذ) + **مسطرة تظلّم** لاستعادة النقاط المسوّئة.
- روابط: greip.io/blog/dictionary/Velocity-Checks-177 · fincoro.com/insights/velocity-checks-fraud-detection · bugnet.io/blog/debugging-leaderboard-anomalies

### و. القواعد الملزمة (تُنفَّذ حرفياً في F6/F7)
1. حدث XP = {UUID, مصدر, مقدار, طابع زمني, مرجع فصل/سؤال, توقيع Ed25519 بالجهاز} — والطابع والUUID داخل النص الموقَّع.
2. `UNIQUE(event_id)` خادمياً = إسناد مزدوج مجاني لكل الحالات.
3. المصادر مرة-واحدة منطقياً: إتمام فصل (أول مرة)، سؤال (أول إجابة صحيحة بالموسم)، مهمة يومية (بسقف يومي موثق).
4. «أعد القراءة» = ٠ نقاط دائماً؛ تقدم القراءة الحقيقي مكانه F3.1 (Drift) لا النقاط.
5. الدوري يُحسب من أحداث **مُتحققة خادمياً فقط** (league_rollup يقرأ الخادم، لا أرقام الأجهزة).
6. عتبات السرعة فوق مئين ٩٩ + نوافذ ٣؛ حظر آلي للقصوى فقط والحدودية لمراجعة الأستاذ مع تظلّم.
7. سحب 10^6 PRNG (قرار ٥٢) لمهام اليوم — ضد «حفظ الأنماط».
8. الخصوصية: لا fingerprint إعلاني؛ هوية الجهاز = مفتاح Ed25519 العمومي حصراً.
