# ٢٦ — خطة إعادة بناء نوط الأستاذ فداء كـ HTML/PDF (NHTML)

> **يُقرأ بعد** `00-CONSTITUTION.md` و`docs/25-HANDOFF-2026-09-16-NOTES.md`.
> هذه الخطة **مستقلة عن مسار تطبيق Flutter**: تعالج عرض نوطة الأستاذ نفسها كنص/رسم، بلا مسّ المضمون.
> النوطة الأولى المستهدفة: **النواسات** (`notes/النواسات م_101932-r23-all-absolute-value-bars-green.docx`).

---

## ١. المشكلة

ملف Word لنوطة الأستاذ معقّد بنيوياً:

| العنصر | العدد (النواسات — مقيس في NHTML-0) |
|---|---|
| فقرات (أعلى المستند) | 1558 |
| فقرات داخل مربعات نصية عائمة | 245 |
| فقرات داخل جداول | 107 |
| معادلات OMML | 1483 |
| كائنات رسومية (صور/أشكال/مربعات/مجموعات) | 399 (منها 383 عائم `wp:anchor`) |
| صور داخل الحزمة | 78 |

محاولات التنسيق داخل Word/python-docx فشلت لأن:
- الرسومات **عائمة** ومرساة داخل فقرات، فتحريك فقرة يحرّك الرسم؛
- `python-docx` لا تعالج `wp:anchor` ولا OMML ولا `txbxContent` بأمان؛
- إضافة `bidi` عامة تقلب اتجاه النص المختلط (عربي + رموز لاتينية/يونانية)؛
- أي إعادة حفظ بمكتبة غير Word تزيح الرسومات وتعيد ترميز الصور.

## ٢. القرار المعتمد

**إعادة بناء النوطة من الصفر كـ HTML + CSS RTL، ثم PDF للطباعة**، مع الاحتفاظ بالـ DOCX الأصلي مرجعاً للمقارنة.

- نسخة الطالب/الطباعة الموثوقة = **PDF** (لا مشاكل تعويم ولا تحرّك).
- المصدر القابل للتعديل = **HTML + CSS**.
- DOCX جديد (مُعاد بناؤه) ممكن لاحقاً، لكن ليس مسار الحقيقة.

## ٣. الممنوع والمسموح

**ممنوع تغييره:** كلمات الأستاذ · الشرح · المعلومات العلمية · ترتيب الأفكار · معنى المعادلات · الرموز · الحلول · المسائل · الرسومات · الصور · الأسهم · المحاور · ألوان الرسومات (إلا التصحيحات المطلوبة سابقاً) · `±` و`∓` · الأقواس والجذور · أي إضافة أو حذف علمي.

**مسموح تغييره (شكل فقط):** توزيع النص · الخط · المسافات · العناوين · الفواصل · شكل الأسئلة · توزيع الرسومات · حجم الرسم مع حفظ النسبة · ألوان عناصر التنسيق الجديدة · ترقيم الصفحات · الغلاف والفهرس (بلا مسّ مضمون) · التحويل إلى HTML/PDF.

## ٤. البنية

```text
rebuild/nawwasat/
├── source/      # نسخة المصدر المجمّدة + بصمتها (لا تُعدَّل)
├── assets/
│   ├── images/      # الصور المستخرجة كما هي (بلا إعادة ضغط)
│   └── equations/   # المعادلات المحوّلة (SVG/صورة) + سجل مصدر كل معادلة
├── content/     # النص الحرفي والجرد (بلا CSS)
├── html/        # index.html + styles.css + print.css
├── pdf/         # PDF بعد التحقق
├── reports/     # تقارير المراحل والتحقق
└── tools/       # سكربتات الجرد/الاستخراج/التحقق/التصدير
```

## ٥. المراحل

| المرحلة | المضمون | الحالة |
|---|---|---|
| **NHTML-0** | جرد المصدر: فقرات، أسئلة، عناوين، معادلات OMML، صور ورسومات (اسم/ترتيب/أبعاد/علاقة/موضع)، manifest + تقرير | ✅ **مكتملة** |
| **NHTML-1** | استخراج النص الحرفي والمعادلات والصور كما هي + كشف الفقرات المفقودة/المكررة + تقرير مقارنة | ✅ **مكتملة** |
| **NHTML-2** | بناء HTML دلالي `lang="ar" dir="rtl"`، كل رسم في `figure`، كل معادلة في عنصر مستقل، بلا `position:absolute` ولا `float` | ✅ **مكتملة** |
| **NHTML-3** | CSS للطباعة A4 (هوامش 18/15mm، منع تداخل الصفحات، `break-inside: avoid`، `orphans/widows`) | ✅ **مكتملة** |
| **NHTML-4** | إخراج PDF عبر Playwright/Chromium بعد `document.fonts.ready` وانتظار الصور | ✅ **مكتملة** |
| NHTML-5 | تحقق نهائي: نص حرفي، صور (عدد/اسم/أبعاد/ترتيب)، معادلات (عدد/رموز/إشارات/وحدات)، فحص بصري لصفحات PDF | ⬜ |

**قواعد إلزامية على المراحل:** لا تُبدأ مرحلة قبل توثيق السابقة؛ كل مرحلة كومِت مستقل على `main`؛ يُحدَّث `docs/26` و`docs/27` بعد كل مرحلة.

## ٦. قواعد البناء الفني (NHTML-2/3)

- `<html lang="ar" dir="rtl">`، والعربية `rtl` داخل `<main dir="rtl" lang="ar">`.
- المعادلات والرموز المختلطة داخل حاوية `dir="ltr"` مع `unicode-bidi: isolate`، واستخدام `<bdi>` عند الحاجة — **لا** `bidi-override` على النص العربي العام.
- كل رسم داخل `<figure>` متمركز، بلا `float` وبلا `position: absolute`، وبلا أكثر من عنصر كبير في طبقة مطلقة.
- `@page { size: A4 portrait; margin: 18mm 15mm 18mm 15mm; }`
- كسر الصفحات: `h1,h2,h3 { break-after: avoid }` و`figure, img, .equation, .question, .table-wrapper { break-inside: avoid }` و`p { orphans: 3; widows: 3 }`.
- الرسم العريض: حاوية مستقلة مع `overflow: visible` وبلا تكبير فوق مساحة الصفحة.
- تصدير PDF: `page.pdf({format:"A4", printBackground:true, preferCSSPageSize:true})` مع انتظار الخطوط والصور — **لا** لقطة شاشة بديلاً عن PDF.

## ٧. التعديلات السابقة الواجب حفظها (كانت في المصدر؟)

مقيسة آلياً في NHTML-0 على النسخة المجمّدة:

| التعديل | الحالة في المصدر |
|---|---|
| تكبير الكرة الزرقاء (r15) | ✅ موجود (`image2.png` معدّل: 0.47% بكسلات مختلفة، أقصى فرق 206) |
| تلوين أعمدة القيمة المطلقة أخضر `008000` في Fmax · vmax · amax · h | ✅ موجود (6 مواضع: r16، r17، r18، r19، r23) |
| تلوين `a` و`v` في الرسم المركّب (r21) ورمز `v` (r20، r22) | ✅ موجود (صور `image4/68/69` معدّلة) |

**تفصيل كامل:** `rebuild/nawwasat/reports/NHTML-0-source-audit.md`.

## ٨. قرار معلّق (يحتاج كلمة المالك)

النسخة المجمّدة في `main` **لا تحمل** تصحيحات الرموز R1–R14 السابقة (سلّمناها سابقاً كملف zip ولم تُلتزم في git):

| القاعدة | مواضع لا تزال بالنمط القديم |
|---|---|
| R2 `ω` بمعنى الثقل → `w` | 37 |
| R3 `∆` U+2206 → `Δ` | 184 |
| R4 `∝` → `α` | 23 |
| R5 `Γ_n` → `Γ_η` | 5 |
| R6 `K` → `k` | 16 |
| R8 `HZ/COS/P_evg/m..g/ⅈ` | 8 |
| R14 `max` ⇄ `maX` (الكتابة مختلطة: `max` 288 · `maX` 21) | 348 |

**قرار المالك:** تطبيق R1–R14 ✅ (نُفِّذ في NHTML-1).

## ٩. قرارات NHTML-2 المنفَّذة (كانت معلّقة)

> **نُفِّذت جميعها** — الدليل الكامل في `rebuild/nawwasat/reports/NHTML-2-structure.md`.

- **صيغة عرض المعادلات:** ✅ **MathML Core** أصلية (تحويل مباشر OMML→MathML بـ lxml، صفر مكتبات خارجية). اختبار عملي نجح: الكسور 717 · الجذور 142 · المحددات `|…|` الملوّنة · `±`/`∓` · `⟸` · الأسّ/الأسفل · `max` مائلة كما في المصدر.
- **الملوّن بالأخضر `008000`:** ✅ انتقل إلى محدّدات القيمة المطلقة — 6 مواضع (`|∓kXmaX|`, `|x=±XmaX|`, …) بلا أي اختلاف لون.
- **بناء «العمودين»:** ✅ 217 شبكة أعمدة (بأعمدة بنسب كثافة النص + حذف الخلايا الفارغة 78 التي كانت تُنتج فراغاً رأسيّاً) — بلا تغيير أي كلمة (تحقّق: 0 حرف مفقود).
- **عناقيد الرسوم:** ✅ 27 عنقوداً؛ الرسوم العائمة أُعيدت عناصر ساكنة في التدفّق بعد فقرتها المرجعية، والصور 78/78 مطابقة بايت-ببايت.
- **إضافات:** `fit-math.js` (تصغير المعادلات الطويلة فقط حتى 55%، بلا مسّ المحتوى) · `tools/build_reference_map.py` (خريطة عنصر↔موضع مصدري، أساس تحقّق NHTML-5) · `tools/verify_html.py` (تحقّق «لا فقدان نصّ» مستقلّ: **0 كتلة غير مغطّاة**).

## ٩-ب. قرارات فنية معلّقة قبل NHTML-2 (للتوثيق التاريخي)

- **صيغة عرض المعادلات:** MathML Core (يدعمه Chromium/Playwright أصلياً، بلا مكتبات خارجية) مقابل KaTeX/SVG مُولَّد مسبقاً. القرار يُتخذ في NHTML-2 مع اختبار عملي على معادلات فيها كسور وجذور ومحددات `|…|` ملوّنة.
- **الملوّن بالأخضر `008000`:** يجب أن ينتقل اللون إلى محددات القيمة المطلقة في 6 مواضع عند العرض.
- **بناء «العمودين»:** 427 فقرة فيها فراغات متعددة (مقارنات جانبية) — تُعاد كشبكة عمودين بلا تغيير كلمة.
- **عناقيد الرسوم:** 25 كتلة تحمل 4 رسومات أو أكثر مرساة إليها — تُعاد كتلة رسم واحدة مرتّبة.

## ١١. نتائج NHTML-2 (مكتملة)

| المقياس | القيمة |
|---|---|
| المخرجات | `html/index.html` (1,307,126 بايت) · `styles.css` · `fit-math.js` · `preview-nhtml2.html` (7.51MB) |
| التغطية | فقرات 1910/1910 · معادلات 1346/1346 · رسوم 399/399 · جداول 6/6 · صور 78/78 مطابقة بايت-ببايت |
| تصحيحات R1–R14 | 443 معادلة (R14: 288 · R3: 184 · R1: 36 · R2: 34 · R4: 23 · R13: 15 · R5: 5 · R8: 4) |
| تحقّق لا-فقدان | **0 كتلة غير مغطّاة · 0 حرف مفقود** (1453 كتلة مفحوصة عبر Chromium) |
| عيوب عُثر عليها وأُصلحت | 6 (تصادم معرّفات الكتل · فقدان خلايا>4 · تكرار mc:AlternateContent · استثناء `*` · مسارات SVG بالملايين · تكبير `fit-math`) |
| سلامة المصدر | البصمة `39dcae28…d35d50` قبل/بعد البناء ✅ |

**المعلّق لـ NHTML-3/4:** توزيع الفجوات البيضاء الناتج عن إعادة تدفّق الرسوم العائمة · تثبيت مقاسات النصوص العربية داخل المعادلات عند الطباعة · لا `print.css` بعد.
**صور المعاينة:** `rebuild/nawwasat/reports/nhtml2-shots/` (01-top · 02-question · 03-lists · 04-cluster · 05-question-mid).

## ١٢. نتائج NHTML-3 (مكتملة)

| المقياس | القيمة |
|---|---|
| الملف | `html/print.css` (A4 · هوامش 18/15/20/15مم · `break-inside: avoid` للوحدات الذرّية · `orphans/widows: 3`) |
| الفحص | `tools/check_print.py` → `content/print-check.json` · 6 لقطات في `reports/nhtml3-shots/` · PDF تشخيصي |
| القصّ/التجاوز الأفقي | **0** · معادلات أوسع من حاويتها **0** · وحدات أطول من الصفحة **0** · فراغات ميتة **0** |
| الصفحات | **101** صفحة A4 (المصدر Word: 66 بإعداداته الأصلية — تخطيط مختلف ورسوم في التدفّق) |
| تذييل الأستاذ | النصّ حرفي من `word/footer1.xml`؛ يُطبع على كل صفحة عبر قالب Chromium في NHTML-4 (تثبيت CSS داخل الصفحة يُقصّ) |
| إصلاح المرحلة | العرض الشريطي للأعمدة (كان عمود أضيق من 120px) ⇒ حدود نسب 22–62% |

## ١٣. نتائج NHTML-4 (مكتملة)

| المقياس | القيمة |
|---|---|
| المخرج | `pdf/نوطة-النواسات.pdf` — **79 صفحة A4** (209.9×297.0مم) · 4.81MB |
| التذييل | سطر الأستاذ حرفياً على **79/79 صفحة** + رقم الصفحة (قالب Chromium · خطّ Naskh مضمَّن) |
| صفحات فارغة | **0** · قصّ **0** · أشكال داكنة **0** |
| الملاءمة | 895 معادلة + 6 جداول مصغَّرة + 3 شبكات حُوّلت صفّاً واحداً |
| الأداة | `tools/make_pdf.py` (لا لقطات شاشة بديلاً عن PDF) |
| مقارنة بالمصدر | Word: 66 صفحة (برسوم عائمة متراكبة) · إعادة البناء: 79 صفحة (لا تراكب ولا قصّ) |
| عيوب أُصلحت | 5: أشكال custGeom السوداء العملاقة · قصّ عمود الجدول · معادلات مضمّنة طويلة · أعمدة ضيّقة · قياس على مقاس خاطئ |

## ١٤. نتائج NHTML-5 (التحقّق النهائي — مكتملة)

| المقياس | القيمة |
|---|---|
| الأداة | `tools/final_validation.py` → `content/final-validation.json` (verdict **7/7 PASS**) |
| النصّ | 591 كتلة مفحوصة · **0 كتلة ناقصة · 0 حرف مفقود** · 12,997 حرفاً عربياً في PDF |
| الصور | **75/75 موضعاً مطابق بصرياً** (بصمة 32×32، Hamming = 0) لـ 75 ملفاً مميّزاً من 78 (2 fallback + 1 يتيمة) |
| المعادلات | 1346 `math` · 142 `msqrt` · 717 `mfrac` · **55 `mover accent` = 55 `m:acc` في المصدر** |
| الرموز | **22/22 رمزاً مطابق** HTML↔PDF: `±`2 `∓`15 `Δ`176 `ω`260 `π`298 `θ`251 `φ`106 `Γ`18 `∑`9 `⟸`11 `⇐`49 `⟹`13 `⇒`60 `→`50 `|`10 · `√` = 142 |
| اللون | `#008000` في المعادلات = 12 · مقاطع خضراء فعلية في PDF = 21 على الصفحات {3,7,9,11,26,50} |
| الترقيم | 79/79 تذييل · أرقام 1→79 متسلسلة · 0 صفحة فارغة/داكنة · A4 موحّد |
| المسح البصري | 7 ألواح تغطّي **كل الصفحات الـ79** + لقطات تكبير (نجوم/محددات/رؤوس أسهم) |
| عيوب أُصلحت (شكل فقط) | 3: رؤوس أسهم مضخّمة (markerWidth 6 → sm/med/lg من المصدر) · 4 نجوم `star5` كانت غير مرسومة (صارت نجمة بلون السمة `#156082`) · تصحيح قياسين زائفين في الفحص (`alttext` و`inline clientWidth`) |
| استكمال R14 | 6 مواضع `max` مقسّمة على مقطعين كانت تفوت القاعدة ⇒ `max` = 0 · `maX` = 296 |
| التزام المصدر | sha256 المصدر ثابت `39dcae28…` قبل/بعد كل تشغيل · لا تغيير كلمة/معادلة/رسم/لون |

## ١٠. أخطاء صُحّحت في أرقام NHTML-0 (شفافية)

| ما ظهر في كومِت NHTML-0 | الصحيح | السبب |
|---|---|---|
| معادلات: 1483 | **1346** | كان النص داخل المربعات النصية يُعدّ مرّتين (في الفقرة الأم وفي كتلته)، وعدد 1483 كان يطابق بالخطأ نسخ fallback المرآتية (137) |
| نص الفقرة الأم يشمل نص مربعها النصي | نص نظيف | مربعات النص تُفهرس ككتل مستقلة لها نصها |
| رسومات غير مرتبطة بكتل (13) | **399/399 مرتبطة** | أبناء المجموعات صاروا يُربطون بكتلة مرساتهم |
