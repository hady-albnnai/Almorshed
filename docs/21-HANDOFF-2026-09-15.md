# ملف التسليم — ٢٠٢٦-٠٩-١٥ (للمحادثة القادمة)

> اقرأ هذا أولاً. المستودع هو مصدر الحقيقة؛ هذا الملف خريطة فقط.

## ١) أين نحن
- الفرع: `arena/01a08df0-almorshed` على `github.com/hady-albnnai/Almorshed`.
- آخر commit مدفوع قبل هذا الملف: `af8edc8` (pack v2). قبله: `4f4cf6e` (U2–U5)، `5fea931` (U1)، `6d245b5` (شاشة التفعيل).
- **المادة العلمية مكتملة** (١٧ درساً من الكتاب الرسمي + ملخصات الأستاذ فداء الأربعة) في `content/authoring/U1..U5/` + `pack.json` مولَّد منها.
- **لم يُشغَّل `flutter test` بعد على الحزمة الجديدة** (لا Flutter في بيئة الوكيل). المالك سيشغّله.

## ٢) المطلوب من المالك الآن (بالترتيب)
```
cd C:\Users\ASUS\Almorshed
git pull
cd app
flutter test
flutter build apk --release
```
- المتوقع: كل الاختبارات خضراء (~٢١٧). إن فشل `content_loader_test.dart` أرسل السطر الأحمر — الاختبار أُعيدت كتابته على الحزمة v2 وتحقَّقت شروطه بايثونياً لا بـDart.
- ثم: حذف التطبيق القديم من الهاتف (شاشة التفعيل تظهر مرة واحدة فقط) وتثبيت الـAPK الجديد.

## ٣) كود التفعيل / كود المراجعة
- **كود المراجعة للأستاذ (فعّال):** `2G9DE-6PH1N-84KBH` — ٥ نقرات على بطاقة الترحيب تفتح وضع المراجعة.
- **كود تفعيل طالب:** يُولَّد عند الطلب (قرار ٣٤) من دالة `office_codes`:
  - PowerShell/CMD مع curl:
    ```
    curl -sS -X POST "https://xdkdgetmztapumcxflop.supabase.co/functions/v1/office_codes" -H "Content-Type: application/json" -H "x-office-key: <قيمة C:\Users\ASUS\.office_key>" -d "{\"action\":\"generate\",\"count\":1}"
    ```
  - الجرد: `{"action":"list","status":"issued"}` · الإبطال: `{"action":"revoke","code":"…"}`.
  - لا تلصق قيمة المفتاح في المحادثة.

## ٤) بنية المحتوى (لمن يتابع)
- `content/authoring/README.md` فهرس عام؛ كل وحدة لها README فيها «نقاط تحتاج تثبيتاً من الأستاذ» (١٦ نقطة إجمالاً).
- القالب الموحد لكل درس: 🔑 → 📖 (+⚠️ تصورات خاطئة) → 🧮 برهان مُرقَّم بدرجات السلم → ✍️ أمثلة ٥/٣/١/١ + `▢` → 🎯 MCQ → 🧾 خلاصة → ⚖️ خصومات → 📎 مقابلة كتاب↔أستاذ↔امتحان.
- وسوم: [أستاذ] / [سلم] / [كتاب] / «مفترض» (رقم غير مقروء في الاستخراج).
- **المولّد:** `python3 tools/md_to_pack.py` يعيد بناء `app/assets/content/pack.json` من المسودات (١٧٩ فقرة، ٦٣ سؤالاً `approved:false`، ١٧١ بطاقة). أعد تشغيله بعد أي تعديل على .md.
- قاموس BAN (٣٠ مصطلحاً في `glossary.json`) يُفحص بـ`contains`: ممنوع «الكثافة» (→ الكتلة الحجمية)، «التيار المستمر» (→ المتواصل)، «نصف ناقل»… المولّد لا يفحص؛ الاختبار يفحص.
- **أرقام «بنية مسألة ٢٠٢٦»** في U2/L5 وU3/L2 **توضيحية** — تُستبدل بأرقام الورقة الرسمية.

## ٥) قرارات محسومة في هذه الجولة
- docs/20 §4.2: **التحريض مُدرَّس** (كان في ملف «الكهرباء» للأستاذ)، **الكهرحراري مُدرَّس**. **الفلكية (U5) مفتوحة** — لا ملخص ولا ظهور ٢٠٢٦ ⇒ موسومة «إثرائية» وتُعرض آخر القائمة حتى يقرر الأستاذ.
- U4: دروس الكتاب السبعة جُمعت في ٣ ملفات (يمكن تفكيكها إلى ٧ في pack إذا طُلب).
- pack v2: ترتيب الوحدات صار بترتيب الكتاب الرسمي (الحزمة القديمة كانت مرتبة خطأ).

## ٦) المفتوح (خارج المادة)
1. `flutter test` + APK + إرسال للأستاذ عبر واتساب (رقم المالك `0938862469`).
2. بعد ملاحظات الأستاذ: مصادقة الأسئلة (`approved:true`)، F6.4 شاشة «حول»، الأيقونة، UI، F6.3 الشهادة.
3. ٥ تجارب مخبرية — غير مكتوبة، بانتظار قرار المالك.
4. `duel_api.dart:93`، بوابة الانتهاء (expiry gate)، المسار ب.

## ٧) قواعد عمل ثابتة (لا تُخالَف)
- المالك على **Windows/CMD**؛ أوامر CMD فقط؛ خطوة واحدة في كل مرة.
- لا PowerShell لنشر الدوال — نشر يدوي من لوحة Supabase؛ مصدر الدالة يُلصق كاملاً في المحادثة عند الحاجة.
- لا طلب قيم سرية أبداً (أسماء فقط). لا تخزين أي token. المالك يعطي PAT مؤقتاً للدفع ويحذفه — لا تذكّره.
- لا تحديث ذاتي للـAPK، لا رفع لأي مكان (F8.4 ملغى). لا لوحة ويب للأستاذ. لا مناقشة تحذيرات security-advisor.
- مصادر الكتاب والأستاذ تبقى خارج المستودع (قرار ٥٥).
- **لا انتظار موافقة على كل خطوة** — اعمل وقدّم تقريراً.
- سطر الإشراف بنصه الحرفي: «تم الإشراف على المادة العلمية من قبل الأستاذ القدير فداء مأمون البني».

## ٨) مطبّات بيئة الوكيل (توفّر وقتاً)
- **حالة git (HEAD/remote) تُمسح بين الجلسات بينما الملفات تبقى** ⇒ قبل أي commit: `git fetch <url> arena/01a08df0-almorshed; git stash -u; git reset --hard FETCH_HEAD; git stash pop` (قد يرفض pop للملفات غير المتتبعة — انسخها إلى /tmp قبلاً وأعدها)، ثم commit بـ`-c user.name="Arena Agent" -c user.email="agent@arena.local"` وpush `HEAD:arena/01a08df0-almorshed`. المستودع عام ⇒ fetch يعمل بلا token؛ push يحتاجه.
- `/tmp` و`~/.local` تُمسحان: `pip install --target /home/user/.local/pylib pymupdf python-docx`؛ Dart SDK غير متوفر عادة.
- ملفات العمل المستخرجة: `/home/user/work/book_u1..u5.txt` (علامات `######## صN`) و`fadaa_*.txt` — خارج المستودع، قد تبقى.


## ملحق — أوراق الدورات المتاحة على الإنترنت (٢٠٢٦-٠٩-١٥)
مُنزَّلة في `/home/user/exams/` (خارج المستودع — قرار ٥٥). المصدر: 3lom4all.com (روابط Drive عامة)، كلها صور مسح ⇒ تحتاج قراءة بصرية لا نصية.

| الدورة | الملفات | الحالة |
|---|---|---|
| ٢٠٢٦ | أسئلة ٦ ص + سلم ١١ ص | ✅ (كانت عندنا) |
| ٢٠٢٥ د١ | أسئلة+حل ٩ ص | ✅ جديدة |
| ٢٠٢٣ د١ | أسئلة (صورتان) + حلول ٣ ملفات + **سلم وزاري ١٢ ص** | ✅ جديدة |
| ٢٠٢٣ د٢ (تكميلي) | صورتان + حل ٧ ص | ✅ جديدة |
| ٢٠٢٢ د١ | أسئلة+حل ٦ ص | ✅ جديدة |
| ٢٠٢٢ د٢ | أسئلة+حل ٧ ص | ✅ جديدة |
| ٢٠٢٤، ٢٠١٨–٢٠٢١ | لم تُعثر بعد على 3lom4all؛ baccalaureate.education (٢٠٢٤) بلا رابط مباشر | ⏳ |
| ٢٠١٣–٢٠١٧ | ملف مجمّع (10.8 MB) على 4readlib/download-scientific-pdf-ebooks — الرابط خلف صفحة تحميل | ⏳ |

الخطوة التالية: نسخ أسئلة كل دورة إلى `content/authoring/exams/<year>-<d>.md` بصيغة الأسئلة القياسية + وسم الوحدة/الدرس، ثم تبويب «أسئلة الدورات».
