# ملف التسليم — مشروع ألمُرشد (Almorshed)
**التاريخ:** 2026-09-22 · **الحالة:** F6.3+F6.4 منجَزة ومدموجة · **قاعدة القبول الحالية: 364 اختبار = أخضر**

**النسخة الرسمية بالمستودع:** `docs/28-HANDOFF-2026-09-22.md` (أودِع بأمر المالك «وثق كل شي بالمستودع»).
> هذا الملف هو المصدر الوحيد المحدَّث لنهاية هذه الجلسة (بطلب المالك: توثيق في ملف تسليم واحد — دون تحميل الكتاب/النوتات على مساحة العمل). الملفات القديمة `05-STATUS-…` و`docs/14` و`docs/22` تبقى كما هي بلا إضافة.

---

## أ) الحالة اللحظية (أرقام وحقائق — لا تخمين)

### المستودع
| البند | القيمة |
|---|---|
| `main` | **`09755b2`** (دمج بموافقة صريحة «نعم ادمج» — ff-only بلا merge-commit) |
| `arena/f-gen2-parts` | **`09755b2`** (نفس الريف) |
| آخر CI على main | run **`35762228498`** 🟢 — analyze **No issues found** · **«تم اختبار 364 بنجاح ✓»** · APK **`fizya-clash-debug-358`** |
| على جهاز المالك | `C:\Users\ASUS\Almorshed` — سحب الفرع و`flutter analyze && flutter test` من داخل `app\` = **364 أخضر محلياً** |
| لطّة هذا الشريحة | `patches/0008-F6.3-F6.4-core.patch` (3 commits) |

**commits هذا الشريحة (3):**
- `d11b5bf` — النواة: `certificate.dart` + `about_screen.dart` + `account_screen` entry + 9 اختبارات + `0014_certificates.sql` + `cert_issue/index.ts` + docs (استثناء الحجب)
- `de402a7` — تصحيح lint واحد (`prefer_const_declarations`)
- `09755b2` — ci.yml: **إضافة annotation «تم اختبار N بنجاح» (لم يكن موجوداً أصلاً!)** + ترميز `esc()` لذيل آخر 25 سطراً (كان يُقطع عند أول سطر)

### سيرفر Supabase (مشروع `xdkdgetmztapumcxflop` — ref عام)
**ما نفّذناه هذه الجلسة خطوة-بخطوة بموافقته (بروتوكول: استعلم/تأكد ← موافقة ← تنفيذ):**

| # | البند | الحالة |
|---|---|---|
| 1 | ترحيل `0014`: جدولا `seasons` + `certificates` + قراءة عامة للجميع + لا إدراج للعميل | ✅ مُنفَّذ ومُتحقَّق (صفّين بـinformation_schema) |
| 2 | صف الموسم: `2026-2027` / `ends_on = 2026-09-21` (**قيمة تجربة — اخترتها أنا بعد «كمل»**) | ✅ مغروس · أمر التبديل: نفس سكربت الـinsert بتاريخ آخر |
| 3 | سرّ `CERT_SK_B64` (بذرة Ed25519 ‏32 بايت base64) | ✅ ولّده بـ`dart run genkey.dart` وحطّه بالداشبورد بنفسه — **القيمة لا تُكتب في الشات ولا بالملفات أبداً** (قرار ٥) |
| 4 | نشر دالة `cert_issue` (#9) | ⏳ **البطاقة سُلِّمته (6 خطوات بالداشبورد) — ينتظر «نشرت»** |

**قائمة الدوال على السيرفر مقابل الكود (فحص ما قبل النشر):**

| الدالة | السيرفر | الكود | الحكم |
|---|---|---|---|
| `heartbeat` | موجودة (updated ~14/9) | آخر تغيير **21/9** | ⚠️ **متأخرة**: ناقصها **F2.2-T2 تشفير `kc_wrap_v1`** |
| `license_activate` | موجودة (~14/9) | **21/9** | ⚠️ **متأخرة**: ناقصها **`kc_wrap_v1`** |
| `office_codes` | موجودة (~14/9) | **22/9** | ⚠️ **متأخرة**: ناقصها **F6.1-POS** |
| `verify_xp_events` | موجودة (~15/9) | 15/9 | ✅ متطابقة |
| `cert_issue` | **غير موجودة** | جديد (هذا الشريحة) | ✅ نشرها = **إنشاء جديد صفر تعارض** — **الأولوية المختارة: «cert_issue أولاً»** |
| `duel_finish` | غير منشورة | 14/9 | خارج مسارنا (تُوثَّق فقط) |

⚠️ **ملاحظة تقنية لـ#10:** `heartbeat` تستورد `../_shared/kc_wrap.ts` ⇒ إعادة نشرها **تحتاج supabase CLI** (محرر الداشبورد ملف واحد لا يفهم الاستيرادات). `cert_issue` مستقلّة (supabase-js + tweetnacl فقط) ⇒ **تُنشر من الداشبورد**.

**حالة البيانات (فحص حيّ):** `devices=7` (تفعيلات تطوير) · `weekly_totals=0` · `certificates=0` · `seasons=1` — التطبيق «تحت التطوير لا يعمل» كما قال المالك ⇒ بوابة الشهادة ترفض الآن بـ«لم تشارك» لكل الأجهزة حتى تجى دورات أسبوعية حقيقية. من يكتب `weekly_totals` = دوال إقفال الأسبوع (`0002`/`0009`/`0011`) تلقائياً.

---

## ب) كل شي وصلناله في هذه الجلسة (إنجازات)

1. **F6.3 نواة الشهادات** — مُنفَّذة رغم حجب docs/22 §١-13 بأمر المالك «تجاهل حجب الاستاذ واكمل» + **الاستثناء موثّق** (docs/22 «استثناء ثانٍ 2026-09-22» + صفّا docs/14).
   - ترتيب حمولـة canonical **متطابق Dart↔TS (8/8) بفحص آلي** · certTitle حرفي «بطل دوري فيزيا كلاش» · طبقتان gold_two/silver_one · Ed25519 nacl · idempotency · أخطاء صريحة · RLS قراءة عامة فقط · 6 اختبارات.
2. **F6.4 شاشة «عن التطبيق»** — شريط قرار 57 (لورانيم + شعار `errorBuilder` / إشراف علمي ذهبي) + الإصدار `0.1.0+1` + دخول `about_entry` من الحساب + 3 اختبارات.
3. **الرقم الرسمي من CI:** annotation «تم اختبار N بنجاح» **أُضيف هذا اليوم** (كان غائباً — العدّ كان استنتاجياً لا من CI) — وكل جلسة قادمة تتحقق منه بلمحة.
4. **364 =355+9** أخضر ثلاث مرات: أول run سقط على lint واحد → أُصلح → أخضر → بعد الدمج أخضر مجدداً (`35762228498`).
5. **الدمج في main بموافقته** — «نعم ادمج» → `main=09755b2`.
6. **بروتوكول المالك الجديد (يلزم الجلسات القادمة):**
   - **«قبل أي تطبيق على السيرفر: استعلم/تأكد/شرح المخاطر ← موافقته الصريحة ← التنفيذ»** (تأكّد مرّتين هذه الجلسة).
   - **واحدة-بلا-كل-مرة**: خطوة واحدة فقط بين يديه حتى ينفّذها ويردّ.
   - **لا قيم سرّية بالشات أبداً** (ناتج المفاتيح يبقى على جهازه فقط).
   - **دمج main بموافقته فقط** · **cmd.exe لا PowerShell** · **لا تخمين نصوص** (نص قرار ٤٠ يبقى فارغاً حتى يأتيه حرفيًّا).
7. **أخطائي التي أُصلحت ودُوّنت (لا تتكرر):** اختراع `Random.nextBytes` (غير موجودة — **تحقق من التوثيق قبل أي API جديد**) · أوامر `echo` أُعطيت وهو بالـSQL Editor (وضّح مكان cmd) · ملاحظة «نسخة أولى/ثانية» مربكة (ما في نسخة غلط — اعتذار وتوضيح) · سكربت تاريخ الموسم اختيرت قيمته التجريبية دون اختياره (**تُعرض الخيارات ولا تُختار له**).

---

## ج) اللي بقي عنا

### على مكتب المالك (الآن)
1. **⏳ #9 نشر `cert_issue`** — بطاقة الـ6 خطوات سُلِّمته (Dashboard: Create function → اسم `cert_issue` → مسح القالب → لصق `supabase\functions\cert_issue\index.ts` من notepad → Deploy → تأكيد الظهور بالقائمة). **السؤال الأول في الجلسة القادمة: «نشرت؟»**
2. بعد النشر: تنظيف `del genkey.dart` (من داخل `app\`).
3. **نصّ قرار ٤٠ الحرفي** — غير موجود في المستودع إطلاقاً (بحث شامل) — يُرسله ليملأ `kFlexibilityNotice40` (Dart) + `NOTICE_40` (TS) ويُ-commit. **حالياً: فارغ = الشهادة بلا تنويه.**

### على الجلسة القادمة (أنا + هو)
4. **#10 إعادة نشر الثلاث المتأخرة** (`heartbeat` + `license_activate` + `office_codes`) — **تحتاج CLI** — واحدة-واحدة بموافقته؛ الترتيب يبدأ بما يختاره (مقترحي: `heartbeat`+`license_activate` لأن `kc_wrap_v1` شرط لتجربة التطبيق لاحقاً).
5. **F6.5 شاشة الشهادة (عرض + مشاركة)** — تكمّل F6 (اليوم: الشهادة تُصدر ولا أحد يراها). الشروط: نشر #9 ناجح · وبيانات `weekly_totals` لتجربة حقيقية (أو مؤجَّل حتى تجى). قرار ٤٠ اختياري (تُعرض بلا تنويه إن لم يأتِ).
   - ⚠️ **فخّ تكامل مسجّل:** `cert_issue` تستقبل JSON خاماً — عند بناء F6.5 تحقّق إن كان النداء المغلّف `kc_wrap` صار معياراً (انظر `verify_xp_events`) والزم التوافق.
6. **متطلب جديد من المالك هذه الجلسة:** **تعديل `ends_on` للموسم من داخل التطبيق** (واجهة إدارة + صلاحيات) — مُسجَّل، لا يُنفَّذ حتى أمره ضمن F6 الباقي.
7. **المؤجَّل المعتاد (لا يُلمس بدون أمر):** تعميم MathText (تحدي/تحدي ليلي/مراجحة/رئيسية) · F-GEN3 §٦٦-6 الباقي · F2.2-T3 (مفاتيحك أنت) · تعديلات الواجهة الكبيرة — **على الحجب** (الاستثناء شمل F6.3/F6.4 فقط) · `duel_finish` غير منشورة (توثيق فقط).

---

## د) بريفنگ المحادثة القادمة (افتح بها)

**سطر الافتتاح المقترح:**
> «كمّلنا من `09755b2` — main نفس الريف، CI أخضر **364**. نشرت `cert_issue` (#9)؟»

**شجرة القرار:**
- **قال «نشرت»** → تحقّق من القائمة (الدوال تصير **5**: +`cert_issue`) → انتقل مباشرة لـ**#10** (إعادة نشر المتأخرة — اسأله: CLI مثبّت؟ `supabase --version`) أو لـ**F6.5** حسب رغبته.
- **قال «لا/لسّا»** → أعِد له **بطاقة #9 كاملة** (6 خطوات، من ملف `supabase\functions\cert_issue\index.ts` المحلي) — لا تتجاوز إلى ما بعدها.
- **أتى بنص قرار ٤٠** → ملأ الطرفين فوراً + commit + push + CI (العدد يبقى 364).
- أي خطوة تمسّ السيرفر → **بروتوكول الاستعلم-أولاً إلزامي**.

**أرقام للتحقق السريع:** 364 اختبار · `main=09755b2` · APK `fizya-clash-debug-358` · run `35762228498` · موسم `2026-2027`/`ends_on=2026-09-21` · 4 دوال منشورة (+`cert_issue` بعد #9).

**ممنوع:** قيم سرّية بالشات أو بالملفات · دمج بلا كلمة «ادمج» · PowerShell · تثبيت Flutter/SDK عندي · إعادة فحص نص قرار ٤٠ داخل المستودع (غائب فعلاً — لا تعيد البحث) · اقتراح بيانات مشاركات مُختلقة (لا تخمين بيانات طلاب).

**ملفات مرجعية:** `patches/0008-F6.3-F6.4-core.patch` · `patches/0014_certificates.sql` (مُستخرَج للّصق) · `patches/0007-mathtext-wired-session.patch` · `05-STATUS-2026-09-22.md` (حتى §١٣ — أرشيف، لا يُحدَّث) · هذا الملف.
