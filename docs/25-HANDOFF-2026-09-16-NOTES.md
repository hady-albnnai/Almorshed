# ملف التسليم — المحادثة القادمة (٢٠٢٦-٠٩-١٦ ليلاً)

> يُقرأ **بعد** `00-CONSTITUTION.md` و`docs/22-HANDOFF-2026-09-15-B.md` (القواعد والقرارات ٣٤–٦٥ ما زالت سارية كلها).
> هذا الملف يغطي فقط ما جرى بعد `docs/22`: **جلسة إعادة التخطيط ونوط الأستاذ**.

---

## ٠. الخلاصة بسطرين
- المالك «أكل بهدلة من الأستاذ» ⇒ **إعادة تخطيط كاملة** مطلوبة، لكن **ملاحظات الأستاذ الحرفية لم تصلنا بعد** — لا تخترعها.
- المنجز الأساسي في هذه الجلسة: **نوط الأستاذ فداء السبع، كما هي حرفياً، برموز معادلات مصحّحة على الكتاب الوزاري** (٩٣٦ موضعاً) + سجل التصحيحات + أداة التصحيح.

---

## ١. الأخطاء المصحّحة في نوط الأستاذ (الأهم)

**المبدأ:** لم يُمسّ أي شرح أو مسألة أو صورة أو تنسيق. التعديل على نصوص المعادلات (`m:t`) فقط، وتحقّقنا آلياً أن فروق الأصل/المصحَّح محصورة بهذه الأحرف.

| # | كان | صار | أين (عدد المواضع) | المرجع |
|---|---|---|---|---|
| R1 | `T_o` (حرف o) | `T_0` | النواسات ×٣٦ | الكتاب: `T₀` |
| R2 | `ω` بمعنى **الثقل** | `w` | النواسات ×٣٤ · المغناطيسية ×١٥ · الإلكترونيات ×٣ | الكتاب ص٢٢ «قوّة الثقل w»؛ `ω` محجوز للنبض |
| R3 | `∆` (رمز الزيادة U+2206) | `Δ` (يونانية) | كل الملفات ×٣٥٥ | الكتاب: `I_Δ` |
| R4 | `∝` (رمز التناسب) مكان التسارع الزاوي/الزاوية | `α` | المغناطيسية ×١٣٨ · النواسات ×٢٣ · الأمواج ×٣ | الكتاب: `α` |
| R5 | `Γ_n` (n لاتينية) | `Γ_η` | النواسات ×٥ · المغناطيسية ×٤ | الكتاب ص٢٢ (η) |
| R6 | `K` لثابت الصلابة/الفتل | `k` | النواسات ×١٤ | الكتاب ص٢١، ص٢٥: «ثابت فتله k»، «ثابت الصلابة k» |
| R7 | `Υ` (أبسلون كبيرة) لمعامل لورنتز | `γ` | النسبية ×٤٠ | النوطة نفسها تستعمل `γ` في الحل |
| R8 | `HZ` · `COS` · `P_evg` · `m..g` · `ⅈ` | `Hz` · `cos` · `P_avg` · `m.g` · `i` | الكهرباء · الأمواج · المغناطيسية (~١٠٠) | أخطاء طباعية |
| R9 | `ωeb` · `ωat` (وحدتان) | `Wb` · `W` | المغناطيسية · الكهرباء ×١٦ | الويبر والواط بحرف W |
| R10 | `Λ` (لامدا كبيرة) للجداء الشعاعي | `∧` | المغناطيسية ×٦ | رمز الجداء الشعاعي |
| R11 | `f_o` · `v_o` · `v_ox` · `v_oy` | `f_0` · `v_0` · `v_0x` · `v_0y` | الكهرباء ×٥ · الإلكترونيات ×٥ | الكتاب: `f₀` |
| R12 | `E_S` · `W_S` | `E_s` · `W_s` | الإلكترونيات ×٣٣ | الكتاب ص٢٣٢: `E_s`، `W_s`، `U_s` |
| R13 | `E_K` · `E_P` | `E_k` · `E_p` | النواسات · النسبية · الإلكترونيات ×٦١ | الكتاب: `E_k`، `E_p` |
| R14 | `Xmax`/`θmax` (كتابة مختلطة) | `XmaX`/`θmaX` | النواسات ×٣٥ · الكهرباء ×٦ | توحيد على الصيغة الغالبة في النوطة |

**حصيلة التعديل لكل ملف:** النواسات ٣٥٠ · المغناطيسية ٣٠٦ · الكهرباء ٨٣ · الإلكترونيات ٧٧ · النسبية ٧٧ · الأمواج ٤٣ · **السوائل (PDF) صفر — رموزها سليمة، نُسخت بلا تعديل.**

**نقطة معلّقة واحدة للأستاذ:** ثابت الفتل — الكتاب `k` وورقة امتحان ٢٠٢٦ كتبته `C`. أبقينا `k` (كالكتاب ونوطته). ملفات التأليف عندنا (`content/authoring/notes/FADAA-NOTE-U1.md`, `2026-1.md`, `U1.yaml`) تستخدم `C` — **توحَّد بعد قراره**.

**ما لا يُصحَّح آلياً (تحتاج عين الأستاذ):** الرموز المكتوبة كصور داخل النوط (٧٧ صورة في النواسات، ١٦٥ في الكهرباء) لم تُمسّ — لا نعرف ما فيها.

---

## ٢. أين الملفات

| ماذا | أين | ملاحظة |
|---|---|---|
| النوط المصحّحة (٦ docx + PDF السوائل + سجل التصحيحات) | جهاز الوكيل `/home/user/fadaa_notes_corrected/` و`/home/user/نوط الأستاذ فداء — رموز مصححة.zip` (٢٠٫٧ MB) | **سلّمناها للمالك بالـzip**. خارج المستودع (قرار ٥٥). قد تُمسح من جهاز الوكيل — يُعاد توليدها بالأداة أدناه |
| نوط الأستاذ الأصلية | git history: `git show 827dbcb^:"sources/fadaa/<اسم>"` (٧ ملفات: النواسات، المغناطيسية، الكهرباء، الأمواج، الإلكترونيات، النسبية الخاصة، السوائل pdf) | أيضاً نص الكتاب: `git show dae20fb:sources/book.norm.txt` |
| **أداة التصحيح** | `tools/fix_fadaa_symbols.py` (كومِت `2d8afc8`) | القواعد R1–R14 موثقة في رأس الملف. تشغيل: تستخرج الأصول إلى `/home/user/fadaa_src/` ثم `python3 tools/fix_fadaa_symbols.py` ⇒ `/home/user/fadaa_notes_corrected/` |
| نوطة U1 المعاد صياغتها (منهجيتنا) | `content/authoring/notes/FADAA-NOTE-U1.md` + `.docx` + المولّد `tools/note_to_docx.py` (كومِت `2b6fb57`) | **المالك رفضها** لأنها بلا شرح الأستاذ — تبقى مرجعاً للقالب فقط، لا تُكمَل U2–U5 بهذه الصيغة إلا إذا طلب |

---

## ٣. ماذا اشتغلنا في هذه الجلسة (بالترتيب)

1. أرسلنا للمالك **فهرس الكتاب الرسمي** (٥ وحدات / ١٧ فصلاً، من `pack.json`) وخريطة **نوط الأستاذ ↔ الفهرس** (نوطة واحدة لكل بحث؛ الأمواج نوطة واحدة لفصلين؛ U5 من الكتاب فقط).
2. راجعنا «نوطة المُرشِد» التي لصقها المالك ⇒ **الحكم: ليست نوطة الأستاذ** (مجمَّعة آلياً): فهرس «سبع وحدات» مخالف للكتاب، ١٢ خطأً فيزيائياً/رمزياً (إشارات φ، `K = IΔ·ω0²` كتعريف، «قوة العطالة النابذة»، `E_s/f_s/λ_s`، …). **القرار: نأخذ قالبها (تنسيقها) فقط.**
3. كتبنا `FADAA-NOTE-U1.md` بذلك القالب (جداول الخطوة⇐الصياغة، تنبيهات السلم، بطاقات تجارب، جداول قوانين) وحوّلناها Word ⇒ المالك: «ليش ما فيها شرح الأستاذ؟» ⇒ طلبه الحرفي: **«النوط الخاصة بالأستاذ متل ما هيي»** + اختار: **تصحيح الرموز فقط، ملفات منفصلة**.
4. استرجعنا نوط الأستاذ الأصلية من git history، حلّلنا معادلاتها OMML (١٤٨٢ كتلة في النواسات وحدها)، بنينا `fix_fadaa_symbols.py`، تحققنا (فتح بـpython-docx + diff حرفي)، سلّمنا الـzip.
5. لم يُرفع أي مصدر إلى GitHub. الفرع `arena/01a08df0-almorshed` رأسه **`2d8afc8`**.

---

## ٤. ماذا سنشتغل (بالترتيب — خطوة واحدة في كل دور)

### المرحلة ٠ — قبل أي تخطيط
1. **اسأل المالك عن ملاحظات الأستاذ الحرفية** («شو قال بالضبط؟ كلمة كلمة») — إعادة التخطيط تبدأ منها ولا تُخترع. سُئل مرتين ولم يجب بعد.
2. اسأل: هل وصلت النوط المصحّحة للأستاذ؟ وما رأيه بالسجل؟ وقراره في `k`/`C`؟

### المرحلة ١ — إعادة التخطيط (بعد الملاحظات)
3. تسجيل الملاحظات كقرار جديد (**٦٦**) في `docs/22 §٢` + `00-CONSTITUTION.md`.
4. احتمالات مرجّحة يجب الاستعداد لها (لا تُنفَّذ قبل التأكيد): (أ) الأستاذ يريد **نوطه** داخل التطبيق لا إعادة صياغتنا ⇒ خط أنابيب docx→محتوى (المعادلات OMML صعبة؛ ربما صور صفحات)؛ (ب) رموز التطبيق `pack.json`/`items_u1.json` توحَّد مع قراره (`k`/`C`، `XmaX`)؛ (ج) اعتراض على منهجية الأسئلة المولَّدة (قرار ٦٤) ⇒ راجع `docs/19`, `docs/20` قبل الرد.

### المرحلة ٢ — ما كان معلّقاً قبل الجلسة (يبقى)
5. نتيجة `flutter test` على `fe6cb92` (شارة 🧪 على بطاقة الفصل) — لم تُبلَّغ قطّ.
6. تسليم الأستاذ: كود مراجعة خاص به (SQL في `docs/22 §٩`/ذاكرة الجلسة؛ **ليس** كود المالك) + APK + `docs/24`.
7. فحص وضع المراجعة (٥ نقرات على «👋 أهلاً بك» خلال ٣ ث).
8. المرحلة B (قرار ٦٤): بقية أجزاء M12، الـ٢٢ ⚠️ في `content/authoring/exams/2026-1.md`، قوالب L4/L5، أوراق ٢٠٢٢/٢٠٢٥، `--pack` بعد موافقة الأستاذ فقط.
9. نظام الجوائز (قرار ٦٥) — **مؤجَّل، لا يُخترع**.
10. المرحلة D (الإغلاق) بعد الأستاذ.

---

## ٥. قواعد جديدة/مؤكَّدة من هذه الجلسة (تُضاف إلى `docs/22 §١`)
- **خطوة واحدة في كل دور — آخر تحذير من المالك.** أمر/إجراء واحد، ثم انتظر نتيجته. لا «إضافات اختيارية».
- **لا تنزيل Flutter SDK** — المالك يشغّل `flutter` على جهازه ويبلّغ.
- عندما يطلب المالك «نوطة الأستاذ» فالمقصود **ملفاته حرفياً**، لا إعادة صياغة — أي تدخل يقتصر على الرموز.
- ردود المالك المكتوبة بحروف لاتينية غير مفهومة (مثل `jl hgjtydg`) = عربي على لوحة مفاتيح إنكليزية («تم التفعيل») — فسّرها بخريطة اللوحة بدل السؤال.
- المصادر (كتاب/نوط/أوراق) خارج المستودع، لكنها **قابلة للاسترجاع من التاريخ** (`827dbcb^`, `dae20fb`) — لا تطلبها من المالك مجدداً.

---

## ٦. مطبّات تقنية من هذه الجلسة
- معادلات النوط **OMML** (`m:oMath`) — استخراجها نصاً يفقد البنية؛ التصحيح الآمن هو تعديل `m:t` داخل الـXML مباشرة (كما تفعل الأداة)، لا إعادة توليد.
- رموز مقسّمة على عدة runs (`P|e|v|g`, `ω|eb`) — الأداة تعالجها بممر تسلسلي؛ أي قاعدة جديدة تحتاج الممرين (النصي والتسلسلي).
- `ω` الثقل مقابل `ω` النبض: التمييز بالسياق (نمط `F_s`, `+T`, `Γ_ω`, `W_ω`, كلمة «ثقل» في فقرة الكتلة) مع استثناء أساس التوابع `ω_0`/`ω_maX`/`sSubSup`. أي ملف جديد يُشغَّل أولاً بـ`--dry` وتُراجَع قائمة R2 يدوياً.
- python-docx: `w:bidi` على الفقرة + `w:bidiVisual` على الجدول ضروريان للعرض RTL؛ المعادلات داخل ` ` تُكتب LTR بخط مونو حتى لا تنقلب الرموز.
- الـzip بأسماء عربية يفتح على Windows 10+ بلا مشكلة (UTF-8 flag).

---

## ٧. أول ما تفعله في المحادثة القادمة
1. رسالة قصيرة: «قرأت التسليم (docs/25). شو قال الأستاذ بالضبط عن التطبيق؟ وشو رأيه بالنوط المصحّحة؟».
2. لا تنفّذ أي إعادة تخطيط قبل جوابه. إن أعطى الملاحظات ⇒ سجّلها حرفياً في `docs/22 §٢` كقرار ٦٦ (خطوة واحدة) ثم اقترح الخطة.
3. اطلب PAT مؤقتاً فقط عند أول حاجة للدفع.
