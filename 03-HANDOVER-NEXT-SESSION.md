# ٠٣ — ملف تسليم — للمحادثة القادمة

> **اقرأ هذا الملف أولاً في أي جلسة قادمة.** كل ما تحتاجه لاستئناف العمل موجود فيه.

---

## ١. ما هو المشروع باختصار

**تطبيق أندرويد اسمه «المرشد»** لطلاب الثالث الثانوي العلمي (البكالوريا) في سوريا، مادة **الفيزياء**، منهاج **دمشق 2026–2027**.

يعطي الطالب: المنهاج الرسمي كاملاً · شرحاً مبسّطاً · ملخّصات · تجارب بأنيميشن · وقبل كل شيء **بنك أسئلة متجدّد لا ينتهي مع سلالم تصحيح تحاكي امتحان الوزارة فعلاً**.

**المرحلة الآن:** تحليل وتخطيط. **لا يوجد أي كود بعد.**

---

## ٢. المستودع

```
https://github.com/hady-albnnai/Almorshed
```

⚠️ **المستودع الآن عام (public) ويحتوي على:**
- ملفات الأستاذ فداء البني السبعة (ملخّصاته المؤلَّفة) — في مجلد `sources/fadaa/`
- وثائق المشروع كاملة

**يجب تحويله إلى private فوراً.** صاحب المشروع يعمل على هذا.

### محتويات المستودع

| الملف | الوصف |
|---|---|
| **`01-PLAN-COMPLETE.md`** | الخطة الكاملة — ابدأ منها |
| **`02-SYMBOLS-TERMS-UNITS.md`** | الرموز والمصطلحات والوحدات — المرجع الملزم (٤٤٤ مدخلاً) |
| **`03-HANDOVER-NEXT-SESSION.md`** | هذا الملف |
| `sources/fadaa/` | ملفات الأستاذ السبعة (٦ docx + 1 pdf) + نصوصها المستخرجة |
| `sources/book-2026-2027.pdf` | الكتاب الرسمي (٢٨٨ صفحة) |
| `sources/exams/` | امتحانات ٢٠١٣–٢٠١٧ + امتحانات ٢٠٢٦ + سلالم التصحيح الرسمية |
| `glossary/` | مولّد القائمة + `review.html` (نموذج المراجعة التفاعلي) + `review.docx` |
| `00-PLAN.md` … `09-FADAA-CROSSCHECK.md` | وثائق التحليل الأصلية (١٠ ملفات، ٢٦١٧ سطراً) |

---

## ٣. أين وصلنا بالضبط

### ✅ مكتمل
| البند | الحالة |
|---|---|
| تحليل الفكرة + الخطة المعمارية | ✅ |
| بحث المادة + جمع الامتحانات الرسمية | ✅ امتحان ٢٠٢٦ + حلوله + سلالم دمشق والوزارة |
| **استنطاق الرموز من الكتاب بالحبر** | ✅ منهجية فريدة — انظر §٥ |
| قائمة المصطلحات | ✅ **٤٤٤ مدخلاً** · صفر تكرار · صفر معلّق |
| مراجعة الأستاذ (الجولة ١) | ✅ ٤٢ تعديلاً + ١٦ إجابة تحكيم |
| **قراءة ملخّصات الأستاذ السبعة** | ✅ ٢٣٦ ألف محرف |
| **حسم البنود الثلاثة المعلّقة** | ✅ كلها محسومة بالدليل |

### 🟡 ينتظر الأستاذ — **تحديث الجلسة الثانية: كلها تحقّقت بالدليل، انظر `docs/10-CONFIRM-TEACHER-ITEMS.md`**
| # | السؤال | الشرح بلغة بسيطة | حالة التحقق (2026-09-11) |
|---|---|---|---|
| ١ | **`Γ_η` أم `Γ_n`؟** | رمز «عزم مزدوجة الفتل». ملف الأستاذ يخزّنه **n إنكليزي** (×10)، والكتاب يرسمه **η يوناني (إيتا)** وهو أملاه. الطالب يرى ما نخزّنه نحن ⇒ اخترنا **η** | ✅ مثبَّت: `η` صفر ورود في كل ملفاته، وملفه يخزّن يونانياً (Γ_ω بـ U+03C9) ⇒ «n» اختيار كتابة لا قيد تخزين. يبقى تأكيد شفهي بلا أثر عملي |
| ٢ | **توحيد رمز الدلتا** | `I_Δ` مخزّن في ملفه بصيغتين مختلفتين بصرياً متطابقتين (U+2206 ×184 مقابل U+0394 ×15) | ✅ محسوم هندسياً: **U+0394** (ما يعتمده المعجم) + تطبيع آلي U+2206→U+0394 في TermLinter. الكتاب يرسم الاثنتين بشكل واحد فلا تعارض ممكن |
| ٣ | **`P_evg` خطأ إملائي** | في ملف «الكهرباء»: عائلة `evg` ×32 مقابل `avg` ×97 (والصحيح `P_avg` الاستطاعة المتوسطة). سنصحّحه تلقائياً | ✅ **مغلق نهائياً**: الكتاب نفسه يكتب `P_avg` ×18 بالعائلة وصفر `evg` في فصل التيار (ص136–167 من نسخة صاحب المشروع) |
| ٤ | **تصنيف ٩ ألفاظ** | الذبذبة · المجال · الزنبرك · عزم القصور · الموجة الموقوفة · أشعة إكس · التأثير الكهروضوئي · المحاثة · الفيض المغناطيسي — «مرادف مسموح» أم «ممنوعة تماماً»؟ | ✅ الأدلة مكتملة من الطرفين: **صفر ورود** للألفاظ التسعة في نصوصه كلها، وبدائل الكتاب عنده بكثافة (الحقل ×97 · النابض ×30 · المستقرة ×29 · العطالة ×26 · السينية ×21 · ذاتية الوشيعة ×8) ⇒ `BAN` — تبقى المصادقة الشكلية |

### ⬜ التالي مباشرة
**مستند الخوارزمية** — التصميم التفصيلي لمحرّك الأسئلة (كيف يُولَّد السؤال، كيف يُنتقى، كيف يُصحَّح) قبل كتابة أي سطر كود.

---

## ٤. القواعد الملزمة (لا تتجاوزها)

### سلسلة المرجعية
```
الكتاب الرسمي  <  قرار الأستاذ فداء  <  لا شيء بعده
```

### الدرجات الثلاث للألفاظ
| الدرجة | القاعدة |
|---|---|
| **رسمي** | إلزامي في نص السؤال وسلّم التصحيح |
| **`SYN` مرادف مسموح** | في الشرح والتلميحات فقط — **ممنوع في نص السؤال** |
| **`BAN` ممنوع** | ظهوره **يفشل البناء** |

### قواعد أخرى
- ❌ **«ما بدي تأكيدات مسبقة»** — لا تضع أي ختم أو تأكيد مسبق في عمود مراجعة الأستاذ. اتركه فارغاً وحيادياً.
- ❌ **لا تخمين** — عند التباس قول الأستاذ ⇒ «معلّق» ويُسأل.
- ✅ الرموز يجب أن تطابق المنهاج الرسمي **حرفياً** — «أهم نقطة» عند صاحب المشروع.
- ✅ التطبيق **أوف لاين بالكامل**.
- ✅ التلميحات **خيار للطالب** (٣ أوضاع).
- ✅ مدة إعادة التحقّق **٣٠ يوماً** (تحذير من اليوم ٢٠).
- ✅ الأسئلة = إحساس «ما بينتهيش» + زر «ولّد المزيد» يعمل **أوف لاين** (مخزون محمّل مسبقاً).
- ✅ **اعتماد ملف الأستاذ فداء مبدئياً** كمصدر الحقيقة حتى يصحى.

---

## ٥. كيف نتحقّق من رمز في الكتاب (منهجية الحبر)

الكتاب يستعمل خطوط **MathMagic**، فالرموز اليونانية **لا تُقرأ من النصّ أبداً**. الطريقة الوحيدة الموثوقة:

```bash
cd /home/user
python3 -c "
import sys; sys.path.insert(0,'/home/user')
from glyph import art
art('project-murshid/sources/book-2026-2027.pdf', 22, 354,695,380,710)
"
```
هذا يقتصّ المنطقة ويطبعها كفنّ ASCII من الحبر فنرى شكل الرمز فعلياً.

**جدول فكّ الترميز (مؤكَّد):**

| المحرف المستخرج | الخط | الرمز الحقيقي |
|---|---|---|
| `C` | MMCenturyOldGreek | **Γ** |
| `D` / `T` | MMCenturyOldGreek / MMExtra | **Δ** |
| `h` | MMCenturyOldGreekItalic | **η** |
| `i` | MMCenturyOldGreekItalic | **θ** |
| `a` | MMCenturyOldGreekItalic | **α** |
| `f` | MMCenturyOldGreek | **Φ** |
| `l` | MMEtc | سهم الشعاع ⃗ |

**ولقراءة ملفات الأستاذ (docx):** الرموز داخل عُقد `m:oMath` وليست في `w:r`. استخرج `m:t` وتابع كل `<m:sSub>` للحصول على الأصل والتابع مع نقاط الترميز الدقيقة.

---

## ٦. أخطاء معروفة — لا تكرّرها

| الخطأ | الصحيح |
|---|---|
| البحث عن Γ في نصوص `w:r` | لا يُرجع شيئاً — الرمز داخل `m:oMath` |
| مقارنة الرموز نصّياً مباشرة | الأستاذ يكتب `Ieff` لا `I_eff` ⇒ طبّع أولاً (احذف `_` والمسافات، ووحّد Δ/∆، وحوّل التوابع) |
| فحص برامج الخطوط لمعرفة شكل الرمز | ميتة (CFF خام بلا أسماء محارف) |
| الاعتماد على OCR يوناني | غير موثوق (ضجيج «ἠ»/«γ») |
| تصديق «Everything up-to-date» بعد خطأ 408 في git | مضلّل — أعد المحاولة |
| أوامر PowerShell للمستخدم | هو على `cmd.exe` ⇒ استعمل `%USERPROFILE%` و `chcp 65001` |
| نماذج archive.org | مصرية («الجهد/المجال/الكهربي») — مرفوضة ومعزولة |

---

## ٧. أوامر جاهزة

```bash
# إعادة بناء القائمة بعد أي تعديل على terms_data.py
cd /home/user/project-murshid/glossary && python3 build_glossary_review.py

# استخراج نصّ من docx جديد للأستاذ
cd /tmp && python3 ext.py file.docx

# رفع تحديث للمستودع
cd C:\Users\ASUS\Desktop\fedaa
git add -A && git commit -m "تحديث" && git push origin main
```
> إن فشل الرفع بـ `HTTP 408`: أعد المحاولة مباشرة. إن تكرّر: `git config http.postBuffer 524288000`.

---

## ٨. خلاصة في سطرين

المشروع في مرحلة **التخطيط المتقدّم**: البحث والاستخراج والتحليل انتهوا، والقائمة المرجعية (٤٤٤ مدخلاً) جاهزة. **بنود انتظار الأستاذ الأربعة أُغلقت كلها بالدليل المزدوج** (`docs/10-CONFIRM-TEACHER-ITEMS.md`) — بما فيها فحوص الكتاب الثلاثة على نسخة صاحب المشروع (P_avg · Φ/ε/μ₀ · Γ_η). ما تبقى تأكيد شفهي مجمّع لا يحرّك عملاً، ثم ننتقل إلى **مستند الخوارزمية** ثم بناء السكيمة، ثم Flutter.
