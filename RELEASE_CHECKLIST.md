# قائمة تجهيز الإطلاق — فيزيا كلاش (dev/self-content)

> الحالة العامة: المنطق التعليمي كامل و**419 اختبار خضراء**. هذه القائمة تتابع
> بنود التسليم/الإنتاج المتبقّية حتى النشر على Google Play.
> آخر تحديث: 2026-09-24.

## مفتاح المسؤولية
- 🤖 = ينفّذه الوكيل على الفرع (كود/إعداد).
- 👤 = يتطلّب جهاز المالك أو أسراره (مفتاح توقيع، تشغيل أدوات، رفع).

---

## 1. هوية التطبيق
- [x] 🤖 اسم التطبيق المعروض (launcher label) عربي بدل `fizya_clash`.
- [x] 🤖 رقم الإصدار الإنتاجي `1.0.0+1` بدل `0.1.0+1`.
- [x] 🤖 أيقونة التطبيق: توليد أيقونة العلامة + إعداد `flutter_launcher_icons`.
- [ ] 👤 تشغيل مولّد الأيقونات على الجهاز: `dart run flutter_launcher_icons`.

> ✅ **مُنجَز (2026-09-24):** صُمِّمت الأيقونة (الخيار «ج»: برق ذهبي `#FBBF24`
> داخل ذرّة بمدارات خضراء `#2DD4A7` على خلفية داكنة `#04222B` = «كلاش الفيزياء»)
> **مع اسم التطبيق «فيزيا كلاش»** أسفل الشعار (خط Cairo ExtraBold ذهبي).
> الشعار+الاسم مُحكَمان داخل منطقة الأمان (لا يُقصّان في الأشكال الدائرية/المُدوّرة).
> الأصول في `app/assets/brand/`:
> - `app_icon.png` (1024²) — الأيقونة القديمة الكاملة (أندرويد < 8).
> - `icon_foreground.png` (شفافة، الشعار+الاسم داخل منطقة أمان) — الطبقة الأمامية التكيّفية.
> - `icon_monochrome.png` — لأيقونات أندرويد 13+ المُوحّدة بالثيم.
> إعداد `flutter_launcher_icons` مضاف في `pubspec.yaml` (خلفية تكيّفية `#04222B`).
> يتبقّى على المالك تشغيل المولّد مرة واحدة (الأمر أدناه) للتحقّق البصري.
> **التالي (بند مستقل):** splash داخلي متحرك (بعد تثبيت الأيقونة على جهاز المالك).

## 2. توقيع الإصدار (release signing)
- [x] 🤖 إعداد `build.gradle.kts` ليقرأ التوقيع من `key.properties` (مُتجاهَل في Git)
      مع تراجع آمن إلى debug إن غاب الملف.
- [x] 🤖 `.gitignore` يتجاهل `key.properties` و`*.jks` و`*.keystore` (كان موجوداً).
- [x] 🤖 قالب `android/key.properties.example` (يُرفع) يوضّح القيم الأربع.
- [ ] 👤 نسخ القالب إلى `android/key.properties` وملؤه بمفتاحك (عندك توقيع جاهز).
      المكان بالضبط: `C:\Users\ASUS\Almorshed\app\android\key.properties`.

> ✅ **مُنجَز (2026-09-24):** المالك ملأ `key.properties` بمفتاح `loraneem.jks`،
> ونجح `flutter build appbundle --release` بإنتاج `app-release.aab` موقّعاً.
> ملاحظة مهمة: يجب أن يكون مسار الـkeystore بحروف **لاتينية** (لا عربية) —
> نُقل المفتاح إلى `D:\Hady\keys\loraneem.jks` لأن Gradle/Java لا يقرأ المسارات
> العربية في ملفات الإعداد.

## ٤ب. شاشة البداية المتحركة (splash)
- [x] 🤖 `AnimatedSplash` أصلية (`AnimationController` + `CustomPainter`): ذرّة
      تتجمّع + ومضة برق ذهبية «كلاش» + ظهور الاسم، ثم دوران هادئ للإلكترونات.
      لا حزم خارجية (Lottie…) — تعمل أوف-لاين، مقدّمة ≤ ١ ثانية.
- [x] 🤖 تُعرض أثناء **التحميل الحقيقي** (حزمة + ترخيص) بلا مؤقّت مصطنع، بدل
      الدوّارة القديمة (`_Splash` في `main.dart`).
- [x] 🤖 تحترم «تقليل الحركة» (`MediaQuery.disableAnimations`) بإطار ثابت (إتاحة).
- [x] 🤖 اختبار مخصّص `test/animated_splash_test.dart` (ثابت + متحرك).
- [ ] 👤 تحقّق بصري عند أول تشغيل (تظهر قبل بوابة التفعيل أثناء التحميل).

## 3. اكتمال الميزات
- [x] 🤖 توصيل تبويب «المراجعة» (كان placeholder «قريباً»).
- [x] 🤖 إزالة إسناد الإشراف العلمي (فرع dev/self-content).
- [x] 🤖 بوابة الاعتماد الوزاري — كل المحتوى معتمد ويصل للطالب.

## 4. جودة وتحقّق
- [x] 🤖 `flutter analyze` نظيف (عدا `genkey.dart` avoid_print — أداة مطوّر).
- [x] 🤖 `flutter test` = 419 اختبار خضراء.
- [ ] 👤 تجربة بصرية يدوية على جهاز حقيقي (RTL، الأسهم/chevron، الأداء).
- [ ] 👤 اختبار على شاشات مختلفة الأحجام.

## 5. متجر Play (المالك)
- [x] 👤 بناء حزمة الإصدار: `flutter build appbundle --release` → `app-release.aab` (2026-09-24).
- [x] 🤖 أيقونة المتجر عالية الدقة 512×512: `store/play_store_icon_512.png`.
- [x] 🤖 نصّ بطاقة المتجر (اسم + وصف مختصر/كامل + قالب سياسة خصوصية): `store/STORE_LISTING.md`.
- [x] 🤖 صورة العرض 1024×500: `store/feature_graphic_1024x500.png`.
- [ ] 👤 لقطات شاشة من الجهاز (٢–٨ صور).
- [ ] 👤 سياسة الخصوصية (رفع القالب على استضافة ووضع الرابط).
- [ ] 👤 **إعادة بناء AAB بالأيقونة الجديدة + الـsplash** ثم الرفع على Play Console والنشر.

## 6. لاحقاً (غير حاجز للإطلاق أوف-لاين)
- [ ] ربط `package_info_plus` بالإصدار المعروض في «عن التطبيق» (F8.1).
- [ ] سيرفر التفعيل/المزامنة F4.4 (`deviceKeyHash`, XP للسيرفر) — التطبيق يعمل
      أوف-لاين بدونها بالكامل.

---

## ملاحظة الفرع
`main` مُجمّد بانتظار الإشراف العلمي. كل ما سبق يجري على `dev/self-content`
(راجع `BRANCHING.md`).
